/*
 * This file has been automatically generated by cffi-grovel.
 * Do not edit it by hand.
 */

#define _GNU_SOURCE
#define _LARGEFILE_SOURCE
#define _LARGEFILE64_SOURCE
#define _FILE_OFFSET_BITS 64
#include <stdlib.h>
#include <errno.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <sys/mman.h>
#include <fcntl.h>
#include <signal.h>
#include <unistd.h>
#include <limits.h>
#include <time.h>
#include <sys/select.h>
#include <sys/poll.h>
#include <sys/ioctl.h>
#include <sys/resource.h>
#include <pwd.h>
#include <grp.h>
#include <dirent.h>
#include <sys/utsname.h>
#include <sys/wait.h>
#include <sys/epoll.h>
#include <sys/syscall.h>
#include <errno.h>

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

#ifndef offsetof
#define offsetof(type, slot) ((int) ((char *) &(((type *) 0)->slot)))
#endif
#define sizeofslot(type, slot) (sizeof(((type *) 0)->slot))
#define stringify(x) #x
#define indirect_stringify(x) stringify(x)

#define SIGNEDP(x) (((x)-1)<0)
#define SIGNED_(x) (SIGNEDP(x)?"":"un")
#define SIGNED64P(x) ( x <= 0x7FFFFFFFFFFFFFFFLL )

void type_name(FILE *output, int signed_p, int size);
char* print_double_for_lisp(double n);

int main(int argc, char**argv) {
  FILE *output = argc > 1 ? fopen(argv[1], "w") : stdout;
  fprintf(output, ";;;; This file has been automatically generated by "
                  "cffi-grovel.\n;;;; Do not edit it by hand.\n\n");
  fprintf(output, "(cl:in-package #:IOLIB.SYSCALLS)\n\n");

  /* ctype section for SIZE-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "size-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(size_t), sizeof(size_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-size-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "size-t");
  fprintf(output, "))\n");

  /* ctype section for SSIZE-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "ssize-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(ssize_t), sizeof(ssize_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-ssize-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "ssize-t");
  fprintf(output, "))\n");

  /* ctype section for PID-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "pid-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(pid_t), sizeof(pid_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-pid-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "pid-t");
  fprintf(output, "))\n");

  /* ctype section for UID-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "uid-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(uid_t), sizeof(uid_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-uid-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "uid-t");
  fprintf(output, "))\n");

  /* ctype section for GID-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "gid-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(gid_t), sizeof(gid_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-gid-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "gid-t");
  fprintf(output, "))\n");

  /* ctype section for OFF-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "off-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(off_t), sizeof(off_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-off-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "off-t");
  fprintf(output, "))\n");

  /* ctype section for MODE-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "mode-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(mode_t), sizeof(mode_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-mode-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "mode-t");
  fprintf(output, "))\n");

  /* constant section for O-RDONLY */
#ifdef O_RDONLY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-rdonly");
  fprintf(output, " ");
  if(SIGNED64P(O_RDONLY))
    fprintf(output, "%lli", (int64_t) O_RDONLY);
  else
    fprintf(output, "%llu", (uint64_t) O_RDONLY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-RDONLY)\n");
#endif

  /* constant section for O-WRONLY */
#ifdef O_WRONLY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-wronly");
  fprintf(output, " ");
  if(SIGNED64P(O_WRONLY))
    fprintf(output, "%lli", (int64_t) O_WRONLY);
  else
    fprintf(output, "%llu", (uint64_t) O_WRONLY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-WRONLY)\n");
#endif

  /* constant section for O-RDWR */
#ifdef O_RDWR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-rdwr");
  fprintf(output, " ");
  if(SIGNED64P(O_RDWR))
    fprintf(output, "%lli", (int64_t) O_RDWR);
  else
    fprintf(output, "%llu", (uint64_t) O_RDWR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-RDWR)\n");
#endif

  /* constant section for O-CREAT */
#ifdef O_CREAT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-creat");
  fprintf(output, " ");
  if(SIGNED64P(O_CREAT))
    fprintf(output, "%lli", (int64_t) O_CREAT);
  else
    fprintf(output, "%llu", (uint64_t) O_CREAT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-CREAT)\n");
#endif

  /* constant section for O-EXCL */
#ifdef O_EXCL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-excl");
  fprintf(output, " ");
  if(SIGNED64P(O_EXCL))
    fprintf(output, "%lli", (int64_t) O_EXCL);
  else
    fprintf(output, "%llu", (uint64_t) O_EXCL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-EXCL)\n");
#endif

  /* constant section for O-TRUNC */
#ifdef O_TRUNC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-trunc");
  fprintf(output, " ");
  if(SIGNED64P(O_TRUNC))
    fprintf(output, "%lli", (int64_t) O_TRUNC);
  else
    fprintf(output, "%llu", (uint64_t) O_TRUNC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-TRUNC)\n");
#endif

  /* constant section for O-APPEND */
#ifdef O_APPEND
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-append");
  fprintf(output, " ");
  if(SIGNED64P(O_APPEND))
    fprintf(output, "%lli", (int64_t) O_APPEND);
  else
    fprintf(output, "%llu", (uint64_t) O_APPEND);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-APPEND)\n");
#endif

  /* constant section for O-NOCTTY */
#ifdef O_NOCTTY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-noctty");
  fprintf(output, " ");
  if(SIGNED64P(O_NOCTTY))
    fprintf(output, "%lli", (int64_t) O_NOCTTY);
  else
    fprintf(output, "%llu", (uint64_t) O_NOCTTY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-NOCTTY)\n");
#endif

  /* constant section for O-NONBLOCK */
#ifdef O_NONBLOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-nonblock");
  fprintf(output, " ");
  if(SIGNED64P(O_NONBLOCK))
    fprintf(output, "%lli", (int64_t) O_NONBLOCK);
  else
    fprintf(output, "%llu", (uint64_t) O_NONBLOCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-NONBLOCK)\n");
#endif

  /* constant section for O-NDELAY */
#ifdef O_NDELAY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-ndelay");
  fprintf(output, " ");
  if(SIGNED64P(O_NDELAY))
    fprintf(output, "%lli", (int64_t) O_NDELAY);
  else
    fprintf(output, "%llu", (uint64_t) O_NDELAY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-NDELAY)\n");
#endif

  /* constant section for O-SYNC */
#ifdef O_SYNC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-sync");
  fprintf(output, " ");
  if(SIGNED64P(O_SYNC))
    fprintf(output, "%lli", (int64_t) O_SYNC);
  else
    fprintf(output, "%llu", (uint64_t) O_SYNC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-SYNC)\n");
#endif

  /* constant section for O-NOFOLLOW */
#ifdef O_NOFOLLOW
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-nofollow");
  fprintf(output, " ");
  if(SIGNED64P(O_NOFOLLOW))
    fprintf(output, "%lli", (int64_t) O_NOFOLLOW);
  else
    fprintf(output, "%llu", (uint64_t) O_NOFOLLOW);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-NOFOLLOW)\n");
#endif

  /* constant section for O-ASYNC */
#ifdef O_ASYNC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-async");
  fprintf(output, " ");
  if(SIGNED64P(O_ASYNC))
    fprintf(output, "%lli", (int64_t) O_ASYNC);
  else
    fprintf(output, "%llu", (uint64_t) O_ASYNC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-ASYNC)\n");
#endif

  /* constant section for O-CLOEXEC */
#ifdef O_CLOEXEC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "o-cloexec");
  fprintf(output, " ");
  if(SIGNED64P(O_CLOEXEC))
    fprintf(output, "%lli", (int64_t) O_CLOEXEC);
  else
    fprintf(output, "%llu", (uint64_t) O_CLOEXEC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'O-CLOEXEC)\n");
#endif

  /* constant section for SEEK-SET */
#ifdef SEEK_SET
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "seek-set");
  fprintf(output, " ");
  if(SIGNED64P(SEEK_SET))
    fprintf(output, "%lli", (int64_t) SEEK_SET);
  else
    fprintf(output, "%llu", (uint64_t) SEEK_SET);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SEEK-SET)\n");
#endif

  /* constant section for SEEK-CUR */
#ifdef SEEK_CUR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "seek-cur");
  fprintf(output, " ");
  if(SIGNED64P(SEEK_CUR))
    fprintf(output, "%lli", (int64_t) SEEK_CUR);
  else
    fprintf(output, "%llu", (uint64_t) SEEK_CUR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SEEK-CUR)\n");
#endif

  /* constant section for SEEK-END */
#ifdef SEEK_END
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "seek-end");
  fprintf(output, " ");
  if(SIGNED64P(SEEK_END))
    fprintf(output, "%lli", (int64_t) SEEK_END);
  else
    fprintf(output, "%llu", (uint64_t) SEEK_END);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SEEK-END)\n");
#endif

  /* constant section for R-OK */
#ifdef R_OK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "r-ok");
  fprintf(output, " ");
  if(SIGNED64P(R_OK))
    fprintf(output, "%lli", (int64_t) R_OK);
  else
    fprintf(output, "%llu", (uint64_t) R_OK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'R-OK)\n");
#endif

  /* constant section for W-OK */
#ifdef W_OK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "w-ok");
  fprintf(output, " ");
  if(SIGNED64P(W_OK))
    fprintf(output, "%lli", (int64_t) W_OK);
  else
    fprintf(output, "%llu", (uint64_t) W_OK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'W-OK)\n");
#endif

  /* constant section for X-OK */
#ifdef X_OK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "x-ok");
  fprintf(output, " ");
  if(SIGNED64P(X_OK))
    fprintf(output, "%lli", (int64_t) X_OK);
  else
    fprintf(output, "%llu", (uint64_t) X_OK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'X-OK)\n");
#endif

  /* constant section for F-OK */
#ifdef F_OK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-ok");
  fprintf(output, " ");
  if(SIGNED64P(F_OK))
    fprintf(output, "%lli", (int64_t) F_OK);
  else
    fprintf(output, "%llu", (uint64_t) F_OK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-OK)\n");
#endif

  /* ctype section for TIME-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "time-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(time_t), sizeof(time_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-time-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "time-t");
  fprintf(output, "))\n");

  /* ctype section for SUSECONDS-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "suseconds-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(suseconds_t), sizeof(suseconds_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-suseconds-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "suseconds-t");
  fprintf(output, "))\n");

  /* ctype section for CLOCKID-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "clockid-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(clockid_t), sizeof(clockid_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-clockid-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "clockid-t");
  fprintf(output, "))\n");

  /* constant section for CLOCK-MONOTONIC */
#ifdef CLOCK_MONOTONIC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "clock-monotonic");
  fprintf(output, " ");
  if(SIGNED64P(CLOCK_MONOTONIC))
    fprintf(output, "%lli", (int64_t) CLOCK_MONOTONIC);
  else
    fprintf(output, "%llu", (uint64_t) CLOCK_MONOTONIC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'CLOCK-MONOTONIC)\n");
#endif

  /* constant section for CLOCK-REALTIME */
#ifdef CLOCK_REALTIME
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "clock-realtime");
  fprintf(output, " ");
  if(SIGNED64P(CLOCK_REALTIME))
    fprintf(output, "%lli", (int64_t) CLOCK_REALTIME);
  else
    fprintf(output, "%llu", (uint64_t) CLOCK_REALTIME);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'CLOCK-REALTIME)\n");
#endif

  /* cstruct section for TIMESPEC */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "timespec");
  fprintf(output, " :size %i)", sizeof(struct timespec));
  fprintf(output, "\n  \"UNIX time specification in seconds and nanoseconds.\"");
  fprintf(output, "\n  (");
  fprintf(output, "sec");
  fprintf(output, " ");
  fprintf(output, "time-t");
  fprintf(output, " :offset %i)", offsetof(struct timespec, tv_sec));
  fprintf(output, "\n  (");
  fprintf(output, "nsec");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct timespec, tv_nsec));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-timespec");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "timespec");
  fprintf(output, "))\n");

  /* ctype section for DEV-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "dev-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(dev_t), sizeof(dev_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-dev-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "dev-t");
  fprintf(output, "))\n");

  /* ctype section for INO-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "ino-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(ino_t), sizeof(ino_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-ino-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "ino-t");
  fprintf(output, "))\n");

  /* ctype section for NLINK-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "nlink-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(nlink_t), sizeof(nlink_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-nlink-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "nlink-t");
  fprintf(output, "))\n");

  /* ctype section for BLKSIZE-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "blksize-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(blksize_t), sizeof(blksize_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-blksize-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "blksize-t");
  fprintf(output, "))\n");

  /* ctype section for BLKCNT-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "blkcnt-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(blkcnt_t), sizeof(blkcnt_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-blkcnt-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "blkcnt-t");
  fprintf(output, "))\n");

  /* cstruct section for STAT */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "stat");
  fprintf(output, " :size %i)", sizeof(struct stat));
  fprintf(output, "\n  (");
  fprintf(output, "dev");
  fprintf(output, " ");
  fprintf(output, "dev-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_dev));
  fprintf(output, "\n  (");
  fprintf(output, "ino");
  fprintf(output, " ");
  fprintf(output, "ino-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_ino));
  fprintf(output, "\n  (");
  fprintf(output, "mode");
  fprintf(output, " ");
  fprintf(output, "mode-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_mode));
  fprintf(output, "\n  (");
  fprintf(output, "nlink");
  fprintf(output, " ");
  fprintf(output, "nlink-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_nlink));
  fprintf(output, "\n  (");
  fprintf(output, "uid");
  fprintf(output, " ");
  fprintf(output, "uid-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_uid));
  fprintf(output, "\n  (");
  fprintf(output, "gid");
  fprintf(output, " ");
  fprintf(output, "gid-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_gid));
  fprintf(output, "\n  (");
  fprintf(output, "rdev");
  fprintf(output, " ");
  fprintf(output, "dev-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_rdev));
  fprintf(output, "\n  (");
  fprintf(output, "size");
  fprintf(output, " ");
  fprintf(output, "off-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_size));
  fprintf(output, "\n  (");
  fprintf(output, "blksize");
  fprintf(output, " ");
  fprintf(output, "blkcnt-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_blksize));
  fprintf(output, "\n  (");
  fprintf(output, "blocks");
  fprintf(output, " ");
  fprintf(output, "blksize-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_blocks));
  fprintf(output, "\n  (");
  fprintf(output, "atime");
  fprintf(output, " ");
  fprintf(output, "time-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_atime));
  fprintf(output, "\n  (");
  fprintf(output, "mtime");
  fprintf(output, " ");
  fprintf(output, "time-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_mtime));
  fprintf(output, "\n  (");
  fprintf(output, "ctime");
  fprintf(output, " ");
  fprintf(output, "time-t");
  fprintf(output, " :offset %i)", offsetof(struct stat, st_ctime));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-stat");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "stat");
  fprintf(output, "))\n");

  /* constant section for +STAT-VERSION+ */
#ifdef _STAT_VER
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "+stat-version+");
  fprintf(output, " ");
  if(SIGNED64P(_STAT_VER))
    fprintf(output, "%lli", (int64_t) _STAT_VER);
  else
    fprintf(output, "%llu", (uint64_t) _STAT_VER);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name '+STAT-VERSION+)\n");
#endif

  /* constant section for S-IRWXU */
#ifdef S_IRWXU
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-irwxu");
  fprintf(output, " ");
  if(SIGNED64P(S_IRWXU))
    fprintf(output, "%lli", (int64_t) S_IRWXU);
  else
    fprintf(output, "%llu", (uint64_t) S_IRWXU);
  fprintf(output, " \"read, write, execute/search by owner\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IRWXU)\n");
#endif

  /* constant section for S-IRUSR */
#ifdef S_IRUSR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-irusr");
  fprintf(output, " ");
  if(SIGNED64P(S_IRUSR))
    fprintf(output, "%lli", (int64_t) S_IRUSR);
  else
    fprintf(output, "%llu", (uint64_t) S_IRUSR);
  fprintf(output, " \"read permission, owner\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IRUSR)\n");
#endif

  /* constant section for S-IWUSR */
#ifdef S_IWUSR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iwusr");
  fprintf(output, " ");
  if(SIGNED64P(S_IWUSR))
    fprintf(output, "%lli", (int64_t) S_IWUSR);
  else
    fprintf(output, "%llu", (uint64_t) S_IWUSR);
  fprintf(output, " \"write permission, owner\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IWUSR)\n");
#endif

  /* constant section for S-IXUSR */
#ifdef S_IXUSR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ixusr");
  fprintf(output, " ");
  if(SIGNED64P(S_IXUSR))
    fprintf(output, "%lli", (int64_t) S_IXUSR);
  else
    fprintf(output, "%llu", (uint64_t) S_IXUSR);
  fprintf(output, " \"execute/search permission, owner\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IXUSR)\n");
#endif

  /* constant section for S-IFMT */
#ifdef S_IFMT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifmt");
  fprintf(output, " ");
  if(SIGNED64P(S_IFMT))
    fprintf(output, "%lli", (int64_t) S_IFMT);
  else
    fprintf(output, "%llu", (uint64_t) S_IFMT);
  fprintf(output, " \"bitmask for type of entry\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFMT)\n");
#endif

  /* constant section for S-IFIFO */
#ifdef S_IFIFO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ififo");
  fprintf(output, " ");
  if(SIGNED64P(S_IFIFO))
    fprintf(output, "%lli", (int64_t) S_IFIFO);
  else
    fprintf(output, "%llu", (uint64_t) S_IFIFO);
  fprintf(output, " \"named pipe, aka fifo\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFIFO)\n");
#endif

  /* constant section for S-IFCHR */
#ifdef S_IFCHR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifchr");
  fprintf(output, " ");
  if(SIGNED64P(S_IFCHR))
    fprintf(output, "%lli", (int64_t) S_IFCHR);
  else
    fprintf(output, "%llu", (uint64_t) S_IFCHR);
  fprintf(output, " \"special character-device\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFCHR)\n");
#endif

  /* constant section for S-IFDIR */
#ifdef S_IFDIR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifdir");
  fprintf(output, " ");
  if(SIGNED64P(S_IFDIR))
    fprintf(output, "%lli", (int64_t) S_IFDIR);
  else
    fprintf(output, "%llu", (uint64_t) S_IFDIR);
  fprintf(output, " \"directory\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFDIR)\n");
#endif

  /* constant section for S-IFBLK */
#ifdef S_IFBLK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifblk");
  fprintf(output, " ");
  if(SIGNED64P(S_IFBLK))
    fprintf(output, "%lli", (int64_t) S_IFBLK);
  else
    fprintf(output, "%llu", (uint64_t) S_IFBLK);
  fprintf(output, " \"special block-device\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFBLK)\n");
#endif

  /* constant section for S-IFREG */
#ifdef S_IFREG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifreg");
  fprintf(output, " ");
  if(SIGNED64P(S_IFREG))
    fprintf(output, "%lli", (int64_t) S_IFREG);
  else
    fprintf(output, "%llu", (uint64_t) S_IFREG);
  fprintf(output, " \"regular file\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFREG)\n");
#endif

  /* constant section for S-IFWHT */
#ifdef S_IFWHT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifwht");
  fprintf(output, " ");
  if(SIGNED64P(S_IFWHT))
    fprintf(output, "%lli", (int64_t) S_IFWHT);
  else
    fprintf(output, "%llu", (uint64_t) S_IFWHT);
  fprintf(output, " \"whiteout\"");
  fprintf(output, ")\n");
#else
#endif

  /* constant section for S-IREAD */
#ifdef S_IREAD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iread");
  fprintf(output, " ");
  if(SIGNED64P(S_IREAD))
    fprintf(output, "%lli", (int64_t) S_IREAD);
  else
    fprintf(output, "%llu", (uint64_t) S_IREAD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IREAD)\n");
#endif

  /* constant section for S-IWRITE */
#ifdef S_IWRITE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iwrite");
  fprintf(output, " ");
  if(SIGNED64P(S_IWRITE))
    fprintf(output, "%lli", (int64_t) S_IWRITE);
  else
    fprintf(output, "%llu", (uint64_t) S_IWRITE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IWRITE)\n");
#endif

  /* constant section for S-IEXEC */
#ifdef S_IEXEC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iexec");
  fprintf(output, " ");
  if(SIGNED64P(S_IEXEC))
    fprintf(output, "%lli", (int64_t) S_IEXEC);
  else
    fprintf(output, "%llu", (uint64_t) S_IEXEC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IEXEC)\n");
#endif

  /* constant section for S-IRWXG */
#ifdef S_IRWXG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-irwxg");
  fprintf(output, " ");
  if(SIGNED64P(S_IRWXG))
    fprintf(output, "%lli", (int64_t) S_IRWXG);
  else
    fprintf(output, "%llu", (uint64_t) S_IRWXG);
  fprintf(output, " \"read, write, execute/search by group\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IRWXG)\n");
#endif

  /* constant section for S-IRGRP */
#ifdef S_IRGRP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-irgrp");
  fprintf(output, " ");
  if(SIGNED64P(S_IRGRP))
    fprintf(output, "%lli", (int64_t) S_IRGRP);
  else
    fprintf(output, "%llu", (uint64_t) S_IRGRP);
  fprintf(output, " \"read permission, group\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IRGRP)\n");
#endif

  /* constant section for S-IWGRP */
#ifdef S_IWGRP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iwgrp");
  fprintf(output, " ");
  if(SIGNED64P(S_IWGRP))
    fprintf(output, "%lli", (int64_t) S_IWGRP);
  else
    fprintf(output, "%llu", (uint64_t) S_IWGRP);
  fprintf(output, " \"write permission, group\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IWGRP)\n");
#endif

  /* constant section for S-IXGRP */
#ifdef S_IXGRP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ixgrp");
  fprintf(output, " ");
  if(SIGNED64P(S_IXGRP))
    fprintf(output, "%lli", (int64_t) S_IXGRP);
  else
    fprintf(output, "%llu", (uint64_t) S_IXGRP);
  fprintf(output, " \"execute/search permission, group\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IXGRP)\n");
#endif

  /* constant section for S-IRWXO */
#ifdef S_IRWXO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-irwxo");
  fprintf(output, " ");
  if(SIGNED64P(S_IRWXO))
    fprintf(output, "%lli", (int64_t) S_IRWXO);
  else
    fprintf(output, "%llu", (uint64_t) S_IRWXO);
  fprintf(output, " \"read, write, execute/search by others\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IRWXO)\n");
#endif

  /* constant section for S-IROTH */
#ifdef S_IROTH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iroth");
  fprintf(output, " ");
  if(SIGNED64P(S_IROTH))
    fprintf(output, "%lli", (int64_t) S_IROTH);
  else
    fprintf(output, "%llu", (uint64_t) S_IROTH);
  fprintf(output, " \"read permission, others\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IROTH)\n");
#endif

  /* constant section for S-IWOTH */
#ifdef S_IWOTH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iwoth");
  fprintf(output, " ");
  if(SIGNED64P(S_IWOTH))
    fprintf(output, "%lli", (int64_t) S_IWOTH);
  else
    fprintf(output, "%llu", (uint64_t) S_IWOTH);
  fprintf(output, " \"write permission, others\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IWOTH)\n");
#endif

  /* constant section for S-IXOTH */
#ifdef S_IXOTH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ixoth");
  fprintf(output, " ");
  if(SIGNED64P(S_IXOTH))
    fprintf(output, "%lli", (int64_t) S_IXOTH);
  else
    fprintf(output, "%llu", (uint64_t) S_IXOTH);
  fprintf(output, " \"execute/search permission, others\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IXOTH)\n");
#endif

  /* constant section for S-ISUID */
#ifdef S_ISUID
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-isuid");
  fprintf(output, " ");
  if(SIGNED64P(S_ISUID))
    fprintf(output, "%lli", (int64_t) S_ISUID);
  else
    fprintf(output, "%llu", (uint64_t) S_ISUID);
  fprintf(output, " \"set-user-ID on execution\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-ISUID)\n");
#endif

  /* constant section for S-ISGID */
#ifdef S_ISGID
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-isgid");
  fprintf(output, " ");
  if(SIGNED64P(S_ISGID))
    fprintf(output, "%lli", (int64_t) S_ISGID);
  else
    fprintf(output, "%llu", (uint64_t) S_ISGID);
  fprintf(output, " \"set-group-ID on execution\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-ISGID)\n");
#endif

  /* constant section for S-ISVTX */
#ifdef S_ISVTX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-isvtx");
  fprintf(output, " ");
  if(SIGNED64P(S_ISVTX))
    fprintf(output, "%lli", (int64_t) S_ISVTX);
  else
    fprintf(output, "%llu", (uint64_t) S_ISVTX);
  fprintf(output, " \"'sticky' bit, many meanings, nonportable\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-ISVTX)\n");
#endif

  /* constant section for S-IFLNK */
#ifdef S_IFLNK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-iflnk");
  fprintf(output, " ");
  if(SIGNED64P(S_IFLNK))
    fprintf(output, "%lli", (int64_t) S_IFLNK);
  else
    fprintf(output, "%llu", (uint64_t) S_IFLNK);
  fprintf(output, " \"symbolic link\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFLNK)\n");
#endif

  /* constant section for S-IFSOCK */
#ifdef S_IFSOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "s-ifsock");
  fprintf(output, " ");
  if(SIGNED64P(S_IFSOCK))
    fprintf(output, "%lli", (int64_t) S_IFSOCK);
  else
    fprintf(output, "%llu", (uint64_t) S_IFSOCK);
  fprintf(output, " \"socket\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'S-IFSOCK)\n");
#endif

  /* constant section for PATH-MAX */
#ifdef PATH_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "path-max");
  fprintf(output, " ");
  if(SIGNED64P(PATH_MAX))
    fprintf(output, "%lli", (int64_t) PATH_MAX);
  else
    fprintf(output, "%llu", (uint64_t) PATH_MAX);
  fprintf(output, ")\n");
#else
#ifdef MAXPATHLEN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "path-max");
  fprintf(output, " ");
  if(SIGNED64P(MAXPATHLEN))
    fprintf(output, "%lli", (int64_t) MAXPATHLEN);
  else
    fprintf(output, "%llu", (uint64_t) MAXPATHLEN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PATH-MAX)\n");
#endif
#endif

  /* constant section for SIGHUP */
#ifdef SIGHUP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sighup");
  fprintf(output, " ");
  if(SIGNED64P(SIGHUP))
    fprintf(output, "%lli", (int64_t) SIGHUP);
  else
    fprintf(output, "%llu", (uint64_t) SIGHUP);
  fprintf(output, " \"terminal line hangup.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGHUP)\n");
#endif

  /* constant section for SIGQUIT */
#ifdef SIGQUIT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigquit");
  fprintf(output, " ");
  if(SIGNED64P(SIGQUIT))
    fprintf(output, "%lli", (int64_t) SIGQUIT);
  else
    fprintf(output, "%llu", (uint64_t) SIGQUIT);
  fprintf(output, " \"quit program.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGQUIT)\n");
#endif

  /* constant section for SIGTRAP */
#ifdef SIGTRAP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigtrap");
  fprintf(output, " ");
  if(SIGNED64P(SIGTRAP))
    fprintf(output, "%lli", (int64_t) SIGTRAP);
  else
    fprintf(output, "%llu", (uint64_t) SIGTRAP);
  fprintf(output, " \"trace trap.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGTRAP)\n");
#endif

  /* constant section for SIGKILL */
#ifdef SIGKILL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigkill");
  fprintf(output, " ");
  if(SIGNED64P(SIGKILL))
    fprintf(output, "%lli", (int64_t) SIGKILL);
  else
    fprintf(output, "%llu", (uint64_t) SIGKILL);
  fprintf(output, " \"kill program.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGKILL)\n");
#endif

  /* constant section for SIGBUS */
#ifdef SIGBUS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigbus");
  fprintf(output, " ");
  if(SIGNED64P(SIGBUS))
    fprintf(output, "%lli", (int64_t) SIGBUS);
  else
    fprintf(output, "%llu", (uint64_t) SIGBUS);
  fprintf(output, " \"bus error.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGBUS)\n");
#endif

  /* constant section for SIGSYS */
#ifdef SIGSYS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigsys");
  fprintf(output, " ");
  if(SIGNED64P(SIGSYS))
    fprintf(output, "%lli", (int64_t) SIGSYS);
  else
    fprintf(output, "%llu", (uint64_t) SIGSYS);
  fprintf(output, " \"non-existent system call invoked.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGSYS)\n");
#endif

  /* constant section for SIGPIPE */
#ifdef SIGPIPE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigpipe");
  fprintf(output, " ");
  if(SIGNED64P(SIGPIPE))
    fprintf(output, "%lli", (int64_t) SIGPIPE);
  else
    fprintf(output, "%llu", (uint64_t) SIGPIPE);
  fprintf(output, " \"write on a pipe with no reader.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGPIPE)\n");
#endif

  /* constant section for SIGALRM */
#ifdef SIGALRM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigalrm");
  fprintf(output, " ");
  if(SIGNED64P(SIGALRM))
    fprintf(output, "%lli", (int64_t) SIGALRM);
  else
    fprintf(output, "%llu", (uint64_t) SIGALRM);
  fprintf(output, " \"real-timeimer expired.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGALRM)\n");
#endif

  /* constant section for SIGURG */
#ifdef SIGURG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigurg");
  fprintf(output, " ");
  if(SIGNED64P(SIGURG))
    fprintf(output, "%lli", (int64_t) SIGURG);
  else
    fprintf(output, "%llu", (uint64_t) SIGURG);
  fprintf(output, " \"urgent condition present on socket.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGURG)\n");
#endif

  /* constant section for SIGSTOP */
#ifdef SIGSTOP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigstop");
  fprintf(output, " ");
  if(SIGNED64P(SIGSTOP))
    fprintf(output, "%lli", (int64_t) SIGSTOP);
  else
    fprintf(output, "%llu", (uint64_t) SIGSTOP);
  fprintf(output, " \"stop (cannot be caught or ignored).\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGSTOP)\n");
#endif

  /* constant section for SIGTSTP */
#ifdef SIGTSTP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigtstp");
  fprintf(output, " ");
  if(SIGNED64P(SIGTSTP))
    fprintf(output, "%lli", (int64_t) SIGTSTP);
  else
    fprintf(output, "%llu", (uint64_t) SIGTSTP);
  fprintf(output, " \"stop signal generated from keyboard.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGTSTP)\n");
#endif

  /* constant section for SIGCONT */
#ifdef SIGCONT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigcont");
  fprintf(output, " ");
  if(SIGNED64P(SIGCONT))
    fprintf(output, "%lli", (int64_t) SIGCONT);
  else
    fprintf(output, "%llu", (uint64_t) SIGCONT);
  fprintf(output, " \"continue after stop.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGCONT)\n");
#endif

  /* constant section for SIGCHLD */
#ifdef SIGCHLD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigchld");
  fprintf(output, " ");
  if(SIGNED64P(SIGCHLD))
    fprintf(output, "%lli", (int64_t) SIGCHLD);
  else
    fprintf(output, "%llu", (uint64_t) SIGCHLD);
  fprintf(output, " \"child status has changed.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGCHLD)\n");
#endif

  /* constant section for SIGCLD */
#ifdef SIGCLD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigcld");
  fprintf(output, " ");
  if(SIGNED64P(SIGCLD))
    fprintf(output, "%lli", (int64_t) SIGCLD);
  else
    fprintf(output, "%llu", (uint64_t) SIGCLD);
  fprintf(output, " \"child status has changed.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGCLD)\n");
#endif

  /* constant section for SIGTTIN */
#ifdef SIGTTIN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigttin");
  fprintf(output, " ");
  if(SIGNED64P(SIGTTIN))
    fprintf(output, "%lli", (int64_t) SIGTTIN);
  else
    fprintf(output, "%llu", (uint64_t) SIGTTIN);
  fprintf(output, " \"background read attempted from control terminal.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGTTIN)\n");
#endif

  /* constant section for SIGTTOU */
#ifdef SIGTTOU
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigttou");
  fprintf(output, " ");
  if(SIGNED64P(SIGTTOU))
    fprintf(output, "%lli", (int64_t) SIGTTOU);
  else
    fprintf(output, "%llu", (uint64_t) SIGTTOU);
  fprintf(output, " \"background write attempted from control terminal.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGTTOU)\n");
#endif

  /* constant section for SIGIO */
#ifdef SIGIO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigio");
  fprintf(output, " ");
  if(SIGNED64P(SIGIO))
    fprintf(output, "%lli", (int64_t) SIGIO);
  else
    fprintf(output, "%llu", (uint64_t) SIGIO);
  fprintf(output, " \"I/O is possible on a descriptor (see fcntl(2)).\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGIO)\n");
#endif

  /* constant section for SIGXCPU */
#ifdef SIGXCPU
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigxcpu");
  fprintf(output, " ");
  if(SIGNED64P(SIGXCPU))
    fprintf(output, "%lli", (int64_t) SIGXCPU);
  else
    fprintf(output, "%llu", (uint64_t) SIGXCPU);
  fprintf(output, " \"cpuime limit exceeded (see setrlimit(2)).\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGXCPU)\n");
#endif

  /* constant section for SIGXFSZ */
#ifdef SIGXFSZ
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigxfsz");
  fprintf(output, " ");
  if(SIGNED64P(SIGXFSZ))
    fprintf(output, "%lli", (int64_t) SIGXFSZ);
  else
    fprintf(output, "%llu", (uint64_t) SIGXFSZ);
  fprintf(output, " \"file size limit exceeded (see setrlimit(2)).\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGXFSZ)\n");
#endif

  /* constant section for SIGVTALRM */
#ifdef SIGVTALRM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigvtalrm");
  fprintf(output, " ");
  if(SIGNED64P(SIGVTALRM))
    fprintf(output, "%lli", (int64_t) SIGVTALRM);
  else
    fprintf(output, "%llu", (uint64_t) SIGVTALRM);
  fprintf(output, " \"virtualime alarm (see setitimer(2)).\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGVTALRM)\n");
#endif

  /* constant section for SIGPROF */
#ifdef SIGPROF
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigprof");
  fprintf(output, " ");
  if(SIGNED64P(SIGPROF))
    fprintf(output, "%lli", (int64_t) SIGPROF);
  else
    fprintf(output, "%llu", (uint64_t) SIGPROF);
  fprintf(output, " \"profilingimer alarm (see setitimer(2)).\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGPROF)\n");
#endif

  /* constant section for SIGWINCH */
#ifdef SIGWINCH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigwinch");
  fprintf(output, " ");
  if(SIGNED64P(SIGWINCH))
    fprintf(output, "%lli", (int64_t) SIGWINCH);
  else
    fprintf(output, "%llu", (uint64_t) SIGWINCH);
  fprintf(output, " \"Window size change.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGWINCH)\n");
#endif

  /* constant section for SIGUSR1 */
#ifdef SIGUSR1
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigusr1");
  fprintf(output, " ");
  if(SIGNED64P(SIGUSR1))
    fprintf(output, "%lli", (int64_t) SIGUSR1);
  else
    fprintf(output, "%llu", (uint64_t) SIGUSR1);
  fprintf(output, " \"User defined signal 1.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGUSR1)\n");
#endif

  /* constant section for SIGUSR2 */
#ifdef SIGUSR2
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigusr2");
  fprintf(output, " ");
  if(SIGNED64P(SIGUSR2))
    fprintf(output, "%lli", (int64_t) SIGUSR2);
  else
    fprintf(output, "%llu", (uint64_t) SIGUSR2);
  fprintf(output, " \"User defined signal 2.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGUSR2)\n");
#endif

  /* constant section for SIGRTMIN */
#ifdef SIGRTMIN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigrtmin");
  fprintf(output, " ");
  if(SIGNED64P(SIGRTMIN))
    fprintf(output, "%lli", (int64_t) SIGRTMIN);
  else
    fprintf(output, "%llu", (uint64_t) SIGRTMIN);
  fprintf(output, " \"Smallest real-time signal number.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGRTMIN)\n");
#endif

  /* constant section for SIGRTMAX */
#ifdef SIGRTMAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sigrtmax");
  fprintf(output, " ");
  if(SIGNED64P(SIGRTMAX))
    fprintf(output, "%lli", (int64_t) SIGRTMAX);
  else
    fprintf(output, "%llu", (uint64_t) SIGRTMAX);
  fprintf(output, " \"Largest real-time signal number.\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIGRTMAX)\n");
#endif

  /* constant section for SIG-IGN */
#ifdef SIG_IGN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sig-ign");
  fprintf(output, " ");
  if(SIGNED64P(SIG_IGN))
    fprintf(output, "%lli", (int64_t) SIG_IGN);
  else
    fprintf(output, "%llu", (uint64_t) SIG_IGN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIG-IGN)\n");
#endif

  /* constant section for SIG-DFL */
#ifdef SIG_DFL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sig-dfl");
  fprintf(output, " ");
  if(SIGNED64P(SIG_DFL))
    fprintf(output, "%lli", (int64_t) SIG_DFL);
  else
    fprintf(output, "%llu", (uint64_t) SIG_DFL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SIG-DFL)\n");
#endif

  /* cstruct section for SIGACTION */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "sigaction");
  fprintf(output, " :size %i)", sizeof(struct sigaction));
  fprintf(output, "\n  (");
  fprintf(output, "handler");
  fprintf(output, " ");
  fprintf(output, ":pointer");
  fprintf(output, " :offset %i)", offsetof(struct sigaction, sa_handler));
  fprintf(output, "\n  (");
  fprintf(output, "sigaction");
  fprintf(output, " ");
  fprintf(output, ":pointer");
  fprintf(output, " :offset %i)", offsetof(struct sigaction, sa_sigaction));
  fprintf(output, "\n  (");
  fprintf(output, "mask");
  fprintf(output, " ");
  fprintf(output, ":unsigned-long");
  fprintf(output, " :offset %i)", offsetof(struct sigaction, sa_mask));
  fprintf(output, "\n  (");
  fprintf(output, "flags");
  fprintf(output, " ");
  fprintf(output, ":int");
  fprintf(output, " :offset %i)", offsetof(struct sigaction, sa_flags));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-sigaction");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "sigaction");
  fprintf(output, "))\n");

  /* constant section for SA-NOCLDSTOP */
#ifdef SA_NOCLDSTOP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-nocldstop");
  fprintf(output, " ");
  if(SIGNED64P(SA_NOCLDSTOP))
    fprintf(output, "%lli", (int64_t) SA_NOCLDSTOP);
  else
    fprintf(output, "%llu", (uint64_t) SA_NOCLDSTOP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-NOCLDSTOP)\n");
#endif

  /* constant section for SA-NOCLDWAIT */
#ifdef SA_NOCLDWAIT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-nocldwait");
  fprintf(output, " ");
  if(SIGNED64P(SA_NOCLDWAIT))
    fprintf(output, "%lli", (int64_t) SA_NOCLDWAIT);
  else
    fprintf(output, "%llu", (uint64_t) SA_NOCLDWAIT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-NOCLDWAIT)\n");
#endif

  /* constant section for SA-NODEFER */
#ifdef SA_NODEFER
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-nodefer");
  fprintf(output, " ");
  if(SIGNED64P(SA_NODEFER))
    fprintf(output, "%lli", (int64_t) SA_NODEFER);
  else
    fprintf(output, "%llu", (uint64_t) SA_NODEFER);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-NODEFER)\n");
#endif

  /* constant section for SA-ONSTACK */
#ifdef SA_ONSTACK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-onstack");
  fprintf(output, " ");
  if(SIGNED64P(SA_ONSTACK))
    fprintf(output, "%lli", (int64_t) SA_ONSTACK);
  else
    fprintf(output, "%llu", (uint64_t) SA_ONSTACK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-ONSTACK)\n");
#endif

  /* constant section for SA-RESETHAND */
#ifdef SA_RESETHAND
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-resethand");
  fprintf(output, " ");
  if(SIGNED64P(SA_RESETHAND))
    fprintf(output, "%lli", (int64_t) SA_RESETHAND);
  else
    fprintf(output, "%llu", (uint64_t) SA_RESETHAND);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-RESETHAND)\n");
#endif

  /* constant section for SA-RESTART */
#ifdef SA_RESTART
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-restart");
  fprintf(output, " ");
  if(SIGNED64P(SA_RESTART))
    fprintf(output, "%lli", (int64_t) SA_RESTART);
  else
    fprintf(output, "%llu", (uint64_t) SA_RESTART);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-RESTART)\n");
#endif

  /* constant section for SA-SIGINFO */
#ifdef SA_SIGINFO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sa-siginfo");
  fprintf(output, " ");
  if(SIGNED64P(SA_SIGINFO))
    fprintf(output, "%lli", (int64_t) SA_SIGINFO);
  else
    fprintf(output, "%llu", (uint64_t) SA_SIGINFO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SA-SIGINFO)\n");
#endif

  /* constant section for SYS-GETTID */
#ifdef SYS_gettid
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sys-gettid");
  fprintf(output, " ");
  if(SIGNED64P(SYS_gettid))
    fprintf(output, "%lli", (int64_t) SYS_gettid);
  else
    fprintf(output, "%llu", (uint64_t) SYS_gettid);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SYS-GETTID)\n");
#endif

  /* ctype section for USECONDS-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "useconds-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(useconds_t), sizeof(useconds_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-useconds-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "useconds-t");
  fprintf(output, "))\n");

  /* cstruct section for TIMEVAL */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "timeval");
  fprintf(output, " :size %i)", sizeof(struct timeval));
  fprintf(output, "\n  \"UNIX time specification in seconds and microseconds.\"");
  fprintf(output, "\n  (");
  fprintf(output, "sec");
  fprintf(output, " ");
  fprintf(output, "time-t");
  fprintf(output, " :offset %i)", offsetof(struct timeval, tv_sec));
  fprintf(output, "\n  (");
  fprintf(output, "usec");
  fprintf(output, " ");
  fprintf(output, "suseconds-t");
  fprintf(output, " :offset %i)", offsetof(struct timeval, tv_usec));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-timeval");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "timeval");
  fprintf(output, "))\n");

  /* constant section for FD-SETSIZE */
#ifdef FD_SETSIZE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "fd-setsize");
  fprintf(output, " ");
  if(SIGNED64P(FD_SETSIZE))
    fprintf(output, "%lli", (int64_t) FD_SETSIZE);
  else
    fprintf(output, "%llu", (uint64_t) FD_SETSIZE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'FD-SETSIZE)\n");
#endif

  /* cstruct section for FD-SET */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "fd-set");
  fprintf(output, " :size %i)", sizeof(fd_set));
  fprintf(output, "\n  (");
  fprintf(output, "bits");
  fprintf(output, " ");
  fprintf(output, ":uint8");
  fprintf(output, " :count %i", sizeof(fd_set) - offsetof(fd_set, fds_bits));
  fprintf(output, " :offset %i)", offsetof(fd_set, fds_bits));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-fd-set");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "fd-set");
  fprintf(output, "))\n");

  /* constant section for F-DUPFD */
#ifdef F_DUPFD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-dupfd");
  fprintf(output, " ");
  if(SIGNED64P(F_DUPFD))
    fprintf(output, "%lli", (int64_t) F_DUPFD);
  else
    fprintf(output, "%llu", (uint64_t) F_DUPFD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-DUPFD)\n");
#endif

  /* constant section for F-GETFD */
#ifdef F_GETFD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-getfd");
  fprintf(output, " ");
  if(SIGNED64P(F_GETFD))
    fprintf(output, "%lli", (int64_t) F_GETFD);
  else
    fprintf(output, "%llu", (uint64_t) F_GETFD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-GETFD)\n");
#endif

  /* constant section for F-SETFD */
#ifdef F_SETFD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setfd");
  fprintf(output, " ");
  if(SIGNED64P(F_SETFD))
    fprintf(output, "%lli", (int64_t) F_SETFD);
  else
    fprintf(output, "%llu", (uint64_t) F_SETFD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETFD)\n");
#endif

  /* constant section for F-GETFL */
#ifdef F_GETFL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-getfl");
  fprintf(output, " ");
  if(SIGNED64P(F_GETFL))
    fprintf(output, "%lli", (int64_t) F_GETFL);
  else
    fprintf(output, "%llu", (uint64_t) F_GETFL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-GETFL)\n");
#endif

  /* constant section for F-SETFL */
#ifdef F_SETFL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setfl");
  fprintf(output, " ");
  if(SIGNED64P(F_SETFL))
    fprintf(output, "%lli", (int64_t) F_SETFL);
  else
    fprintf(output, "%llu", (uint64_t) F_SETFL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETFL)\n");
#endif

  /* constant section for F-GETLK */
#ifdef F_GETLK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-getlk");
  fprintf(output, " ");
  if(SIGNED64P(F_GETLK))
    fprintf(output, "%lli", (int64_t) F_GETLK);
  else
    fprintf(output, "%llu", (uint64_t) F_GETLK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-GETLK)\n");
#endif

  /* constant section for F-SETLK */
#ifdef F_SETLK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setlk");
  fprintf(output, " ");
  if(SIGNED64P(F_SETLK))
    fprintf(output, "%lli", (int64_t) F_SETLK);
  else
    fprintf(output, "%llu", (uint64_t) F_SETLK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETLK)\n");
#endif

  /* constant section for F-SETLKW */
#ifdef F_SETLKW
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setlkw");
  fprintf(output, " ");
  if(SIGNED64P(F_SETLKW))
    fprintf(output, "%lli", (int64_t) F_SETLKW);
  else
    fprintf(output, "%llu", (uint64_t) F_SETLKW);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETLKW)\n");
#endif

  /* constant section for F-GETOWN */
#ifdef F_GETOWN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-getown");
  fprintf(output, " ");
  if(SIGNED64P(F_GETOWN))
    fprintf(output, "%lli", (int64_t) F_GETOWN);
  else
    fprintf(output, "%llu", (uint64_t) F_GETOWN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-GETOWN)\n");
#endif

  /* constant section for F-SETOWN */
#ifdef F_SETOWN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setown");
  fprintf(output, " ");
  if(SIGNED64P(F_SETOWN))
    fprintf(output, "%lli", (int64_t) F_SETOWN);
  else
    fprintf(output, "%llu", (uint64_t) F_SETOWN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETOWN)\n");
#endif

  /* constant section for F-RDLCK */
#ifdef F_RDLCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-rdlck");
  fprintf(output, " ");
  if(SIGNED64P(F_RDLCK))
    fprintf(output, "%lli", (int64_t) F_RDLCK);
  else
    fprintf(output, "%llu", (uint64_t) F_RDLCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-RDLCK)\n");
#endif

  /* constant section for F-WRLCK */
#ifdef F_WRLCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-wrlck");
  fprintf(output, " ");
  if(SIGNED64P(F_WRLCK))
    fprintf(output, "%lli", (int64_t) F_WRLCK);
  else
    fprintf(output, "%llu", (uint64_t) F_WRLCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-WRLCK)\n");
#endif

  /* constant section for F-UNLCK */
#ifdef F_UNLCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-unlck");
  fprintf(output, " ");
  if(SIGNED64P(F_UNLCK))
    fprintf(output, "%lli", (int64_t) F_UNLCK);
  else
    fprintf(output, "%llu", (uint64_t) F_UNLCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-UNLCK)\n");
#endif

  /* constant section for F-GETSIG */
#ifdef F_GETSIG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-getsig");
  fprintf(output, " ");
  if(SIGNED64P(F_GETSIG))
    fprintf(output, "%lli", (int64_t) F_GETSIG);
  else
    fprintf(output, "%llu", (uint64_t) F_GETSIG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-GETSIG)\n");
#endif

  /* constant section for F-SETSIG */
#ifdef F_SETSIG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setsig");
  fprintf(output, " ");
  if(SIGNED64P(F_SETSIG))
    fprintf(output, "%lli", (int64_t) F_SETSIG);
  else
    fprintf(output, "%llu", (uint64_t) F_SETSIG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETSIG)\n");
#endif

  /* constant section for F-SETLEASE */
#ifdef F_SETLEASE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-setlease");
  fprintf(output, " ");
  if(SIGNED64P(F_SETLEASE))
    fprintf(output, "%lli", (int64_t) F_SETLEASE);
  else
    fprintf(output, "%llu", (uint64_t) F_SETLEASE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-SETLEASE)\n");
#endif

  /* constant section for F-GETLEASE */
#ifdef F_GETLEASE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "f-getlease");
  fprintf(output, " ");
  if(SIGNED64P(F_GETLEASE))
    fprintf(output, "%lli", (int64_t) F_GETLEASE);
  else
    fprintf(output, "%llu", (uint64_t) F_GETLEASE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'F-GETLEASE)\n");
#endif

  /* constant section for PROT-NONE */
#ifdef PROT_NONE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prot-none");
  fprintf(output, " ");
  if(SIGNED64P(PROT_NONE))
    fprintf(output, "%lli", (int64_t) PROT_NONE);
  else
    fprintf(output, "%llu", (uint64_t) PROT_NONE);
  fprintf(output, " \"mmap: no protection\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PROT-NONE)\n");
#endif

  /* constant section for PROT-READ */
#ifdef PROT_READ
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prot-read");
  fprintf(output, " ");
  if(SIGNED64P(PROT_READ))
    fprintf(output, "%lli", (int64_t) PROT_READ);
  else
    fprintf(output, "%llu", (uint64_t) PROT_READ);
  fprintf(output, " \"mmap: read protection\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PROT-READ)\n");
#endif

  /* constant section for PROT-WRITE */
#ifdef PROT_WRITE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prot-write");
  fprintf(output, " ");
  if(SIGNED64P(PROT_WRITE))
    fprintf(output, "%lli", (int64_t) PROT_WRITE);
  else
    fprintf(output, "%llu", (uint64_t) PROT_WRITE);
  fprintf(output, " \"mmap: write protection\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PROT-WRITE)\n");
#endif

  /* constant section for PROT-EXEC */
#ifdef PROT_EXEC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prot-exec");
  fprintf(output, " ");
  if(SIGNED64P(PROT_EXEC))
    fprintf(output, "%lli", (int64_t) PROT_EXEC);
  else
    fprintf(output, "%llu", (uint64_t) PROT_EXEC);
  fprintf(output, " \"mmap: execute protection\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PROT-EXEC)\n");
#endif

  /* constant section for MAP-SHARED */
#ifdef MAP_SHARED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "map-shared");
  fprintf(output, " ");
  if(SIGNED64P(MAP_SHARED))
    fprintf(output, "%lli", (int64_t) MAP_SHARED);
  else
    fprintf(output, "%llu", (uint64_t) MAP_SHARED);
  fprintf(output, " \"mmap: shared memory\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'MAP-SHARED)\n");
#endif

  /* constant section for MAP-PRIVATE */
#ifdef MAP_PRIVATE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "map-private");
  fprintf(output, " ");
  if(SIGNED64P(MAP_PRIVATE))
    fprintf(output, "%lli", (int64_t) MAP_PRIVATE);
  else
    fprintf(output, "%llu", (uint64_t) MAP_PRIVATE);
  fprintf(output, " \"mmap: private mapping\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'MAP-PRIVATE)\n");
#endif

  /* constant section for MAP-FIXED */
#ifdef MAP_FIXED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "map-fixed");
  fprintf(output, " ");
  if(SIGNED64P(MAP_FIXED))
    fprintf(output, "%lli", (int64_t) MAP_FIXED);
  else
    fprintf(output, "%llu", (uint64_t) MAP_FIXED);
  fprintf(output, " \"mmap: map at location\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'MAP-FIXED)\n");
#endif

  /* constant section for MAP-FAILED */
#ifdef MAP_FAILED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "map-failed");
  fprintf(output, " ");
  if(SIGNED64P(MAP_FAILED))
    fprintf(output, "%lli", (int64_t) MAP_FAILED);
  else
    fprintf(output, "%llu", (uint64_t) MAP_FAILED);
  fprintf(output, " \"mmap: failure\"");
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'MAP-FAILED)\n");
#endif

  /* ctype section for NFDS-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "nfds-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(nfds_t), sizeof(nfds_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-nfds-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "nfds-t");
  fprintf(output, "))\n");

  /* cstruct section for POLLFD */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "pollfd");
  fprintf(output, " :size %i)", sizeof(struct pollfd));
  fprintf(output, "\n  \"Poll file descriptor activity specification structure.\"");
  fprintf(output, "\n  (");
  fprintf(output, "fd");
  fprintf(output, " ");
  fprintf(output, ":int");
  fprintf(output, " :offset %i)", offsetof(struct pollfd, fd));
  fprintf(output, "\n  (");
  fprintf(output, "events");
  fprintf(output, " ");
  fprintf(output, ":short");
  fprintf(output, " :offset %i)", offsetof(struct pollfd, events));
  fprintf(output, "\n  (");
  fprintf(output, "revents");
  fprintf(output, " ");
  fprintf(output, ":short");
  fprintf(output, " :offset %i)", offsetof(struct pollfd, revents));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-pollfd");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "pollfd");
  fprintf(output, "))\n");

  /* constant section for POLLIN */
#ifdef POLLIN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollin");
  fprintf(output, " ");
  if(SIGNED64P(POLLIN))
    fprintf(output, "%lli", (int64_t) POLLIN);
  else
    fprintf(output, "%llu", (uint64_t) POLLIN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLIN)\n");
#endif

  /* constant section for POLLRDNORM */
#ifdef POLLRDNORM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollrdnorm");
  fprintf(output, " ");
  if(SIGNED64P(POLLRDNORM))
    fprintf(output, "%lli", (int64_t) POLLRDNORM);
  else
    fprintf(output, "%llu", (uint64_t) POLLRDNORM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLRDNORM)\n");
#endif

  /* constant section for POLLRDBAND */
#ifdef POLLRDBAND
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollrdband");
  fprintf(output, " ");
  if(SIGNED64P(POLLRDBAND))
    fprintf(output, "%lli", (int64_t) POLLRDBAND);
  else
    fprintf(output, "%llu", (uint64_t) POLLRDBAND);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLRDBAND)\n");
#endif

  /* constant section for POLLPRI */
#ifdef POLLPRI
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollpri");
  fprintf(output, " ");
  if(SIGNED64P(POLLPRI))
    fprintf(output, "%lli", (int64_t) POLLPRI);
  else
    fprintf(output, "%llu", (uint64_t) POLLPRI);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLPRI)\n");
#endif

  /* constant section for POLLOUT */
#ifdef POLLOUT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollout");
  fprintf(output, " ");
  if(SIGNED64P(POLLOUT))
    fprintf(output, "%lli", (int64_t) POLLOUT);
  else
    fprintf(output, "%llu", (uint64_t) POLLOUT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLOUT)\n");
#endif

  /* constant section for POLLWRNORM */
#ifdef POLLWRNORM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollwrnorm");
  fprintf(output, " ");
  if(SIGNED64P(POLLWRNORM))
    fprintf(output, "%lli", (int64_t) POLLWRNORM);
  else
    fprintf(output, "%llu", (uint64_t) POLLWRNORM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLWRNORM)\n");
#endif

  /* constant section for POLLWRBAND */
#ifdef POLLWRBAND
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollwrband");
  fprintf(output, " ");
  if(SIGNED64P(POLLWRBAND))
    fprintf(output, "%lli", (int64_t) POLLWRBAND);
  else
    fprintf(output, "%llu", (uint64_t) POLLWRBAND);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLWRBAND)\n");
#endif

  /* constant section for POLLERR */
#ifdef POLLERR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollerr");
  fprintf(output, " ");
  if(SIGNED64P(POLLERR))
    fprintf(output, "%lli", (int64_t) POLLERR);
  else
    fprintf(output, "%llu", (uint64_t) POLLERR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLERR)\n");
#endif

  /* constant section for POLLRDHUP */
#ifdef POLLRDHUP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollrdhup");
  fprintf(output, " ");
  if(SIGNED64P(POLLRDHUP))
    fprintf(output, "%lli", (int64_t) POLLRDHUP);
  else
    fprintf(output, "%llu", (uint64_t) POLLRDHUP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLRDHUP)\n");
#endif

  /* constant section for POLLHUP */
#ifdef POLLHUP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollhup");
  fprintf(output, " ");
  if(SIGNED64P(POLLHUP))
    fprintf(output, "%lli", (int64_t) POLLHUP);
  else
    fprintf(output, "%llu", (uint64_t) POLLHUP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLHUP)\n");
#endif

  /* constant section for POLLNVAL */
#ifdef POLLNVAL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "pollnval");
  fprintf(output, " ");
  if(SIGNED64P(POLLNVAL))
    fprintf(output, "%lli", (int64_t) POLLNVAL);
  else
    fprintf(output, "%llu", (uint64_t) POLLNVAL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'POLLNVAL)\n");
#endif

  /* cunion section for EPOLL-DATA */
  fprintf(output, "(cffi:defcunion (");
  fprintf(output, "epoll-data");
  fprintf(output, " :size %i)", sizeof(epoll_data_t));
  fprintf(output, "\n  (");
  fprintf(output, "ptr");
  fprintf(output, " ");
  fprintf(output, ":pointer");
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, "fd");
  fprintf(output, " ");
  fprintf(output, ":int");
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, "u32");
  fprintf(output, " ");
  fprintf(output, ":uint32");
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, "u64");
  fprintf(output, " ");
  fprintf(output, ":uint64");
  fprintf(output, ")");
  fprintf(output, ")\n");

  /* cstruct section for EPOLL-EVENT */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "epoll-event");
  fprintf(output, " :size %i)", sizeof(struct epoll_event));
  fprintf(output, "\n  (");
  fprintf(output, "events");
  fprintf(output, " ");
  fprintf(output, ":uint32");
  fprintf(output, " :offset %i)", offsetof(struct epoll_event, events));
  fprintf(output, "\n  (");
  fprintf(output, "data");
  fprintf(output, " ");
  fprintf(output, "epoll-data");
  fprintf(output, " :offset %i)", offsetof(struct epoll_event, data));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-epoll-event");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "epoll-event");
  fprintf(output, "))\n");

  /* constant section for EPOLL-CTL-ADD */
#ifdef EPOLL_CTL_ADD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epoll-ctl-add");
  fprintf(output, " ");
  if(SIGNED64P(EPOLL_CTL_ADD))
    fprintf(output, "%lli", (int64_t) EPOLL_CTL_ADD);
  else
    fprintf(output, "%llu", (uint64_t) EPOLL_CTL_ADD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLL-CTL-ADD)\n");
#endif

  /* constant section for EPOLL-CTL-DEL */
#ifdef EPOLL_CTL_DEL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epoll-ctl-del");
  fprintf(output, " ");
  if(SIGNED64P(EPOLL_CTL_DEL))
    fprintf(output, "%lli", (int64_t) EPOLL_CTL_DEL);
  else
    fprintf(output, "%llu", (uint64_t) EPOLL_CTL_DEL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLL-CTL-DEL)\n");
#endif

  /* constant section for EPOLL-CTL-MOD */
#ifdef EPOLL_CTL_MOD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epoll-ctl-mod");
  fprintf(output, " ");
  if(SIGNED64P(EPOLL_CTL_MOD))
    fprintf(output, "%lli", (int64_t) EPOLL_CTL_MOD);
  else
    fprintf(output, "%llu", (uint64_t) EPOLL_CTL_MOD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLL-CTL-MOD)\n");
#endif

  /* constant section for EPOLLIN */
#ifdef EPOLLIN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollin");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLIN))
    fprintf(output, "%lli", (int64_t) EPOLLIN);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLIN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLIN)\n");
#endif

  /* constant section for EPOLLRDNORM */
#ifdef EPOLLRDNORM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollrdnorm");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLRDNORM))
    fprintf(output, "%lli", (int64_t) EPOLLRDNORM);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLRDNORM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLRDNORM)\n");
#endif

  /* constant section for EPOLLRDBAND */
#ifdef EPOLLRDBAND
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollrdband");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLRDBAND))
    fprintf(output, "%lli", (int64_t) EPOLLRDBAND);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLRDBAND);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLRDBAND)\n");
#endif

  /* constant section for EPOLLPRI */
#ifdef EPOLLPRI
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollpri");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLPRI))
    fprintf(output, "%lli", (int64_t) EPOLLPRI);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLPRI);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLPRI)\n");
#endif

  /* constant section for EPOLLOUT */
#ifdef EPOLLOUT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollout");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLOUT))
    fprintf(output, "%lli", (int64_t) EPOLLOUT);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLOUT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLOUT)\n");
#endif

  /* constant section for EPOLLWRNORM */
#ifdef EPOLLWRNORM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollwrnorm");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLWRNORM))
    fprintf(output, "%lli", (int64_t) EPOLLWRNORM);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLWRNORM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLWRNORM)\n");
#endif

  /* constant section for EPOLLWRBAND */
#ifdef EPOLLWRBAND
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollwrband");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLWRBAND))
    fprintf(output, "%lli", (int64_t) EPOLLWRBAND);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLWRBAND);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLWRBAND)\n");
#endif

  /* constant section for EPOLLERR */
#ifdef EPOLLERR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollerr");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLERR))
    fprintf(output, "%lli", (int64_t) EPOLLERR);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLERR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLERR)\n");
#endif

  /* constant section for EPOLLHUP */
#ifdef EPOLLHUP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollhup");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLHUP))
    fprintf(output, "%lli", (int64_t) EPOLLHUP);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLHUP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLHUP)\n");
#endif

  /* constant section for EPOLLMSG */
#ifdef EPOLLMSG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollmsg");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLMSG))
    fprintf(output, "%lli", (int64_t) EPOLLMSG);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLMSG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLMSG)\n");
#endif

  /* constant section for EPOLLONESHOT */
#ifdef EPOLLONESHOT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epolloneshot");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLONESHOT))
    fprintf(output, "%lli", (int64_t) EPOLLONESHOT);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLONESHOT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLONESHOT)\n");
#endif

  /* constant section for EPOLLET */
#ifdef EPOLLET
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epollet");
  fprintf(output, " ");
  if(SIGNED64P(EPOLLET))
    fprintf(output, "%lli", (int64_t) EPOLLET);
  else
    fprintf(output, "%llu", (uint64_t) EPOLLET);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPOLLET)\n");
#endif

  /* cstruct section for DIRENT */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "dirent");
  fprintf(output, " :size %i)", sizeof(struct dirent));
  fprintf(output, "\n  (");
  fprintf(output, "fileno");
  fprintf(output, " ");
  fprintf(output, "ino-t");
  fprintf(output, " :offset %i)", offsetof(struct dirent, d_fileno));
  fprintf(output, "\n  (");
  fprintf(output, "type");
  fprintf(output, " ");
  fprintf(output, ":uint8");
  fprintf(output, " :offset %i)", offsetof(struct dirent, d_type));
  fprintf(output, "\n  (");
  fprintf(output, "name");
  fprintf(output, " ");
  fprintf(output, ":uint8");
  fprintf(output, " :count %i", sizeof(struct dirent) - offsetof(struct dirent, d_name));
  fprintf(output, " :offset %i)", offsetof(struct dirent, d_name));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-dirent");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "dirent");
  fprintf(output, "))\n");

  /* constant section for DT-UNKNOWN */
#ifdef DT_UNKNOWN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-unknown");
  fprintf(output, " ");
  if(SIGNED64P(DT_UNKNOWN))
    fprintf(output, "%lli", (int64_t) DT_UNKNOWN);
  else
    fprintf(output, "%llu", (uint64_t) DT_UNKNOWN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-UNKNOWN)\n");
#endif

  /* constant section for DT-FIFO */
#ifdef DT_FIFO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-fifo");
  fprintf(output, " ");
  if(SIGNED64P(DT_FIFO))
    fprintf(output, "%lli", (int64_t) DT_FIFO);
  else
    fprintf(output, "%llu", (uint64_t) DT_FIFO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-FIFO)\n");
#endif

  /* constant section for DT-CHR */
#ifdef DT_CHR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-chr");
  fprintf(output, " ");
  if(SIGNED64P(DT_CHR))
    fprintf(output, "%lli", (int64_t) DT_CHR);
  else
    fprintf(output, "%llu", (uint64_t) DT_CHR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-CHR)\n");
#endif

  /* constant section for DT-DIR */
#ifdef DT_DIR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-dir");
  fprintf(output, " ");
  if(SIGNED64P(DT_DIR))
    fprintf(output, "%lli", (int64_t) DT_DIR);
  else
    fprintf(output, "%llu", (uint64_t) DT_DIR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-DIR)\n");
#endif

  /* constant section for DT-BLK */
#ifdef DT_BLK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-blk");
  fprintf(output, " ");
  if(SIGNED64P(DT_BLK))
    fprintf(output, "%lli", (int64_t) DT_BLK);
  else
    fprintf(output, "%llu", (uint64_t) DT_BLK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-BLK)\n");
#endif

  /* constant section for DT-REG */
#ifdef DT_REG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-reg");
  fprintf(output, " ");
  if(SIGNED64P(DT_REG))
    fprintf(output, "%lli", (int64_t) DT_REG);
  else
    fprintf(output, "%llu", (uint64_t) DT_REG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-REG)\n");
#endif

  /* constant section for DT-LNK */
#ifdef DT_LNK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-lnk");
  fprintf(output, " ");
  if(SIGNED64P(DT_LNK))
    fprintf(output, "%lli", (int64_t) DT_LNK);
  else
    fprintf(output, "%llu", (uint64_t) DT_LNK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-LNK)\n");
#endif

  /* constant section for DT-SOCK */
#ifdef DT_SOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-sock");
  fprintf(output, " ");
  if(SIGNED64P(DT_SOCK))
    fprintf(output, "%lli", (int64_t) DT_SOCK);
  else
    fprintf(output, "%llu", (uint64_t) DT_SOCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-SOCK)\n");
#endif

  /* constant section for DT-WHT */
#ifdef DT_WHT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "dt-wht");
  fprintf(output, " ");
  if(SIGNED64P(DT_WHT))
    fprintf(output, "%lli", (int64_t) DT_WHT);
  else
    fprintf(output, "%llu", (uint64_t) DT_WHT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'DT-WHT)\n");
#endif

  /* constant section for FIONBIO */
#ifdef FIONBIO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "fionbio");
  fprintf(output, " ");
  if(SIGNED64P(FIONBIO))
    fprintf(output, "%lli", (int64_t) FIONBIO);
  else
    fprintf(output, "%llu", (uint64_t) FIONBIO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'FIONBIO)\n");
#endif

  /* constant section for FIONREAD */
#ifdef FIONREAD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "fionread");
  fprintf(output, " ");
  if(SIGNED64P(FIONREAD))
    fprintf(output, "%lli", (int64_t) FIONREAD);
  else
    fprintf(output, "%llu", (uint64_t) FIONREAD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'FIONREAD)\n");
#endif

  /* ctype section for RLIM-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "rlim-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(rlim_t), sizeof(rlim_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-rlim-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "rlim-t");
  fprintf(output, "))\n");

  /* ctype section for ID-T */
  fprintf(output, "(cffi:defctype ");
  fprintf(output, "id-t");
  fprintf(output, " ");
  type_name(output, SIGNEDP(id_t), sizeof(id_t));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-id-t");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "id-t");
  fprintf(output, "))\n");

  /* cstruct section for RLIMIT */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "rlimit");
  fprintf(output, " :size %i)", sizeof(struct rlimit));
  fprintf(output, "\n  (");
  fprintf(output, "cur");
  fprintf(output, " ");
  fprintf(output, "rlim-t");
  fprintf(output, " :offset %i)", offsetof(struct rlimit, rlim_cur));
  fprintf(output, "\n  (");
  fprintf(output, "max");
  fprintf(output, " ");
  fprintf(output, "rlim-t");
  fprintf(output, " :offset %i)", offsetof(struct rlimit, rlim_max));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-rlimit");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "rlimit");
  fprintf(output, "))\n");

  /* cstruct section for RUSAGE */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "rusage");
  fprintf(output, " :size %i)", sizeof(struct rusage));
  fprintf(output, "\n  (");
  fprintf(output, "utime");
  fprintf(output, " ");
  fprintf(output, "timeval");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_utime));
  fprintf(output, "\n  (");
  fprintf(output, "stime");
  fprintf(output, " ");
  fprintf(output, "timeval");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_stime));
  fprintf(output, "\n  (");
  fprintf(output, "maxrss");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_maxrss));
  fprintf(output, "\n  (");
  fprintf(output, "ixrss");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_ixrss));
  fprintf(output, "\n  (");
  fprintf(output, "idrss");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_idrss));
  fprintf(output, "\n  (");
  fprintf(output, "isrss");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_isrss));
  fprintf(output, "\n  (");
  fprintf(output, "minflt");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_minflt));
  fprintf(output, "\n  (");
  fprintf(output, "majflt");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_majflt));
  fprintf(output, "\n  (");
  fprintf(output, "nswap");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_nswap));
  fprintf(output, "\n  (");
  fprintf(output, "inblock");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_inblock));
  fprintf(output, "\n  (");
  fprintf(output, "oublock");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_oublock));
  fprintf(output, "\n  (");
  fprintf(output, "msgsnd");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_msgsnd));
  fprintf(output, "\n  (");
  fprintf(output, "msgrcv");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_msgrcv));
  fprintf(output, "\n  (");
  fprintf(output, "nsignals");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_nsignals));
  fprintf(output, "\n  (");
  fprintf(output, "nvcsw");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_nvcsw));
  fprintf(output, "\n  (");
  fprintf(output, "nivcsw");
  fprintf(output, " ");
  fprintf(output, ":long");
  fprintf(output, " :offset %i)", offsetof(struct rusage, ru_nivcsw));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-rusage");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "rusage");
  fprintf(output, "))\n");

  /* constant section for PRIO-PROCESS */
#ifdef PRIO_PROCESS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prio-process");
  fprintf(output, " ");
  if(SIGNED64P(PRIO_PROCESS))
    fprintf(output, "%lli", (int64_t) PRIO_PROCESS);
  else
    fprintf(output, "%llu", (uint64_t) PRIO_PROCESS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PRIO-PROCESS)\n");
#endif

  /* constant section for PRIO-PGRP */
#ifdef PRIO_PGRP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prio-pgrp");
  fprintf(output, " ");
  if(SIGNED64P(PRIO_PGRP))
    fprintf(output, "%lli", (int64_t) PRIO_PGRP);
  else
    fprintf(output, "%llu", (uint64_t) PRIO_PGRP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PRIO-PGRP)\n");
#endif

  /* constant section for PRIO-USER */
#ifdef PRIO_USER
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "prio-user");
  fprintf(output, " ");
  if(SIGNED64P(PRIO_USER))
    fprintf(output, "%lli", (int64_t) PRIO_USER);
  else
    fprintf(output, "%llu", (uint64_t) PRIO_USER);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'PRIO-USER)\n");
#endif

  /* constant section for RLIM-INFINITY */
#ifdef RLIM_INFINITY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlim-infinity");
  fprintf(output, " ");
  if(SIGNED64P(RLIM_INFINITY))
    fprintf(output, "%lli", (int64_t) RLIM_INFINITY);
  else
    fprintf(output, "%llu", (uint64_t) RLIM_INFINITY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIM-INFINITY)\n");
#endif

  /* constant section for RUSAGE-SELF */
#ifdef RUSAGE_SELF
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rusage-self");
  fprintf(output, " ");
  if(SIGNED64P(RUSAGE_SELF))
    fprintf(output, "%lli", (int64_t) RUSAGE_SELF);
  else
    fprintf(output, "%llu", (uint64_t) RUSAGE_SELF);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RUSAGE-SELF)\n");
#endif

  /* constant section for RUSAGE-CHILDREN */
#ifdef RUSAGE_CHILDREN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rusage-children");
  fprintf(output, " ");
  if(SIGNED64P(RUSAGE_CHILDREN))
    fprintf(output, "%lli", (int64_t) RUSAGE_CHILDREN);
  else
    fprintf(output, "%llu", (uint64_t) RUSAGE_CHILDREN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RUSAGE-CHILDREN)\n");
#endif

  /* constant section for RLIMIT-AS */
#ifdef RLIMIT_AS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-as");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_AS))
    fprintf(output, "%lli", (int64_t) RLIMIT_AS);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_AS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-AS)\n");
#endif

  /* constant section for RLIMIT-CORE */
#ifdef RLIMIT_CORE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-core");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_CORE))
    fprintf(output, "%lli", (int64_t) RLIMIT_CORE);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_CORE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-CORE)\n");
#endif

  /* constant section for RLIMIT-CPU */
#ifdef RLIMIT_CPU
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-cpu");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_CPU))
    fprintf(output, "%lli", (int64_t) RLIMIT_CPU);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_CPU);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-CPU)\n");
#endif

  /* constant section for RLIMIT-DATA */
#ifdef RLIMIT_DATA
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-data");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_DATA))
    fprintf(output, "%lli", (int64_t) RLIMIT_DATA);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_DATA);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-DATA)\n");
#endif

  /* constant section for RLIMIT-FSIZE */
#ifdef RLIMIT_FSIZE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-fsize");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_FSIZE))
    fprintf(output, "%lli", (int64_t) RLIMIT_FSIZE);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_FSIZE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-FSIZE)\n");
#endif

  /* constant section for RLIMIT-MEMLOCK */
#ifdef RLIMIT_MEMLOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-memlock");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_MEMLOCK))
    fprintf(output, "%lli", (int64_t) RLIMIT_MEMLOCK);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_MEMLOCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-MEMLOCK)\n");
#endif

  /* constant section for RLIMIT-NOFILE */
#ifdef RLIMIT_NOFILE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-nofile");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_NOFILE))
    fprintf(output, "%lli", (int64_t) RLIMIT_NOFILE);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_NOFILE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-NOFILE)\n");
#endif

  /* constant section for RLIMIT-NPROC */
#ifdef RLIMIT_NPROC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-nproc");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_NPROC))
    fprintf(output, "%lli", (int64_t) RLIMIT_NPROC);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_NPROC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-NPROC)\n");
#endif

  /* constant section for RLIMIT-RSS */
#ifdef RLIMIT_RSS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-rss");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_RSS))
    fprintf(output, "%lli", (int64_t) RLIMIT_RSS);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_RSS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-RSS)\n");
#endif

  /* constant section for RLIMIT-STACK */
#ifdef RLIMIT_STACK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-stack");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_STACK))
    fprintf(output, "%lli", (int64_t) RLIMIT_STACK);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_STACK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-STACK)\n");
#endif

  /* constant section for RLIM-SAVED-MAX */
#ifdef RLIM_SAVED_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlim-saved-max");
  fprintf(output, " ");
  if(SIGNED64P(RLIM_SAVED_MAX))
    fprintf(output, "%lli", (int64_t) RLIM_SAVED_MAX);
  else
    fprintf(output, "%llu", (uint64_t) RLIM_SAVED_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIM-SAVED-MAX)\n");
#endif

  /* constant section for RLIM-SAVED-CUR */
#ifdef RLIM_SAVED_CUR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlim-saved-cur");
  fprintf(output, " ");
  if(SIGNED64P(RLIM_SAVED_CUR))
    fprintf(output, "%lli", (int64_t) RLIM_SAVED_CUR);
  else
    fprintf(output, "%llu", (uint64_t) RLIM_SAVED_CUR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIM-SAVED-CUR)\n");
#endif

  /* constant section for RLIMIT-LOCKS */
#ifdef RLIMIT_LOCKS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-locks");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_LOCKS))
    fprintf(output, "%lli", (int64_t) RLIMIT_LOCKS);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_LOCKS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-LOCKS)\n");
#endif

  /* constant section for RLIMIT-MSGQUEUE */
#ifdef RLIMIT_MSGQUEUE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-msgqueue");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_MSGQUEUE))
    fprintf(output, "%lli", (int64_t) RLIMIT_MSGQUEUE);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_MSGQUEUE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-MSGQUEUE)\n");
#endif

  /* constant section for RLIMIT-NLIMITS */
#ifdef RLIMIT_NLIMITS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-nlimits");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_NLIMITS))
    fprintf(output, "%lli", (int64_t) RLIMIT_NLIMITS);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_NLIMITS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-NLIMITS)\n");
#endif

  /* constant section for RLIMIT-NICE */
#ifdef RLIMIT_NICE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-nice");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_NICE))
    fprintf(output, "%lli", (int64_t) RLIMIT_NICE);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_NICE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-NICE)\n");
#endif

  /* constant section for RLIMIT-RTPRIO */
#ifdef RLIMIT_RTPRIO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-rtprio");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_RTPRIO))
    fprintf(output, "%lli", (int64_t) RLIMIT_RTPRIO);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_RTPRIO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-RTPRIO)\n");
#endif

  /* constant section for RLIMIT-SIGPENDING */
#ifdef RLIMIT_SIGPENDING
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "rlimit-sigpending");
  fprintf(output, " ");
  if(SIGNED64P(RLIMIT_SIGPENDING))
    fprintf(output, "%lli", (int64_t) RLIMIT_SIGPENDING);
  else
    fprintf(output, "%llu", (uint64_t) RLIMIT_SIGPENDING);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'RLIMIT-SIGPENDING)\n");
#endif

  /* cstruct section for UTSNAME */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "utsname");
  fprintf(output, " :size %i)", sizeof(struct utsname));
  fprintf(output, "\n  (");
  fprintf(output, "sysname");
  fprintf(output, " ");
  fprintf(output, ":char");
  fprintf(output, " :offset %i)", offsetof(struct utsname, sysname));
  fprintf(output, "\n  (");
  fprintf(output, "nodename");
  fprintf(output, " ");
  fprintf(output, ":char");
  fprintf(output, " :offset %i)", offsetof(struct utsname, nodename));
  fprintf(output, "\n  (");
  fprintf(output, "release");
  fprintf(output, " ");
  fprintf(output, ":char");
  fprintf(output, " :offset %i)", offsetof(struct utsname, release));
  fprintf(output, "\n  (");
  fprintf(output, "version");
  fprintf(output, " ");
  fprintf(output, ":char");
  fprintf(output, " :offset %i)", offsetof(struct utsname, version));
  fprintf(output, "\n  (");
  fprintf(output, "machine");
  fprintf(output, " ");
  fprintf(output, ":char");
  fprintf(output, " :offset %i)", offsetof(struct utsname, machine));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-utsname");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "utsname");
  fprintf(output, "))\n");

  /* cstruct section for PASSWD-ENTRY */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "passwd-entry");
  fprintf(output, " :size %i)", sizeof(struct passwd));
  fprintf(output, "\n  (");
  fprintf(output, "name");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_name));
  fprintf(output, "\n  (");
  fprintf(output, "passwd");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_passwd));
  fprintf(output, "\n  (");
  fprintf(output, "uid");
  fprintf(output, " ");
  fprintf(output, "uid-t");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_uid));
  fprintf(output, "\n  (");
  fprintf(output, "gid");
  fprintf(output, " ");
  fprintf(output, "gid-t");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_gid));
  fprintf(output, "\n  (");
  fprintf(output, "gecos");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_gecos));
  fprintf(output, "\n  (");
  fprintf(output, "dir");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_dir));
  fprintf(output, "\n  (");
  fprintf(output, "shell");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct passwd, pw_shell));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-passwd-entry");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "passwd-entry");
  fprintf(output, "))\n");

  /* cstruct section for GROUP-ENTRY */
  fprintf(output, "(cffi:defcstruct (");
  fprintf(output, "group-entry");
  fprintf(output, " :size %i)", sizeof(struct group));
  fprintf(output, "\n  (");
  fprintf(output, "name");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct group, gr_name));
  fprintf(output, "\n  (");
  fprintf(output, "passwd");
  fprintf(output, " ");
  fprintf(output, "sstring");
  fprintf(output, " :offset %i)", offsetof(struct group, gr_passwd));
  fprintf(output, "\n  (");
  fprintf(output, "gid");
  fprintf(output, " ");
  fprintf(output, "gid-t");
  fprintf(output, " :offset %i)", offsetof(struct group, gr_gid));
  fprintf(output, "\n  (");
  fprintf(output, "mem");
  fprintf(output, " ");
  fprintf(output, ":pointer");
  fprintf(output, " :offset %i)", offsetof(struct group, gr_mem));
  fprintf(output, ")\n");
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "size-of-group-entry");
  fprintf(output, " (cffi:foreign-type-size '");
  fprintf(output, "group-entry");
  fprintf(output, "))\n");

  /* constantenum section for ERRNO-VALUES */
  fprintf(output, "(cffi:defcenum (");
  fprintf(output, "errno-values");
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eperm");
#ifdef EPERM
  fprintf(output, " ");
  fprintf(output, "%i", EPERM);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EPERM) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enoent");
#ifdef ENOENT
  fprintf(output, " ");
  fprintf(output, "%i", ENOENT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOENT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":esrch");
#ifdef ESRCH
  fprintf(output, " ");
  fprintf(output, "%i", ESRCH);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ESRCH) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eintr");
#ifdef EINTR
  fprintf(output, " ");
  fprintf(output, "%i", EINTR);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EINTR) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eio");
#ifdef EIO
  fprintf(output, " ");
  fprintf(output, "%i", EIO);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EIO) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enxio");
#ifdef ENXIO
  fprintf(output, " ");
  fprintf(output, "%i", ENXIO);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENXIO) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":e2big");
#ifdef E2BIG
  fprintf(output, " ");
  fprintf(output, "%i", E2BIG);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'E2BIG) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enoexec");
#ifdef ENOEXEC
  fprintf(output, " ");
  fprintf(output, "%i", ENOEXEC);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOEXEC) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebadf");
#ifdef EBADF
  fprintf(output, " ");
  fprintf(output, "%i", EBADF);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EBADF) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":echild");
#ifdef ECHILD
  fprintf(output, " ");
  fprintf(output, "%i", ECHILD);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ECHILD) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enomem");
#ifdef ENOMEM
  fprintf(output, " ");
  fprintf(output, "%i", ENOMEM);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOMEM) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eacces");
#ifdef EACCES
  fprintf(output, " ");
  fprintf(output, "%i", EACCES);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EACCES) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":efault");
#ifdef EFAULT
  fprintf(output, " ");
  fprintf(output, "%i", EFAULT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EFAULT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebusy");
#ifdef EBUSY
  fprintf(output, " ");
  fprintf(output, "%i", EBUSY);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EBUSY) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eexist");
#ifdef EEXIST
  fprintf(output, " ");
  fprintf(output, "%i", EEXIST);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EEXIST) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":exdev");
#ifdef EXDEV
  fprintf(output, " ");
  fprintf(output, "%i", EXDEV);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EXDEV) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enodev");
#ifdef ENODEV
  fprintf(output, " ");
  fprintf(output, "%i", ENODEV);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENODEV) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotdir");
#ifdef ENOTDIR
  fprintf(output, " ");
  fprintf(output, "%i", ENOTDIR);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTDIR) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eisdir");
#ifdef EISDIR
  fprintf(output, " ");
  fprintf(output, "%i", EISDIR);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EISDIR) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":einval");
#ifdef EINVAL
  fprintf(output, " ");
  fprintf(output, "%i", EINVAL);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EINVAL) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enfile");
#ifdef ENFILE
  fprintf(output, " ");
  fprintf(output, "%i", ENFILE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENFILE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":emfile");
#ifdef EMFILE
  fprintf(output, " ");
  fprintf(output, "%i", EMFILE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EMFILE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotty");
#ifdef ENOTTY
  fprintf(output, " ");
  fprintf(output, "%i", ENOTTY);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTTY) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":efbig");
#ifdef EFBIG
  fprintf(output, " ");
  fprintf(output, "%i", EFBIG);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EFBIG) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enospc");
#ifdef ENOSPC
  fprintf(output, " ");
  fprintf(output, "%i", ENOSPC);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOSPC) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":espipe");
#ifdef ESPIPE
  fprintf(output, " ");
  fprintf(output, "%i", ESPIPE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ESPIPE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":erofs");
#ifdef EROFS
  fprintf(output, " ");
  fprintf(output, "%i", EROFS);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EROFS) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":emlink");
#ifdef EMLINK
  fprintf(output, " ");
  fprintf(output, "%i", EMLINK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EMLINK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":epipe");
#ifdef EPIPE
  fprintf(output, " ");
  fprintf(output, "%i", EPIPE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EPIPE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":edom");
#ifdef EDOM
  fprintf(output, " ");
  fprintf(output, "%i", EDOM);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EDOM) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":erange");
#ifdef ERANGE
  fprintf(output, " ");
  fprintf(output, "%i", ERANGE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ERANGE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":edeadlk");
#ifdef EDEADLK
  fprintf(output, " ");
  fprintf(output, "%i", EDEADLK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EDEADLK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enametoolong");
#ifdef ENAMETOOLONG
  fprintf(output, " ");
  fprintf(output, "%i", ENAMETOOLONG);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENAMETOOLONG) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enolck");
#ifdef ENOLCK
  fprintf(output, " ");
  fprintf(output, "%i", ENOLCK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOLCK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enosys");
#ifdef ENOSYS
  fprintf(output, " ");
  fprintf(output, "%i", ENOSYS);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOSYS) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotempty");
#ifdef ENOTEMPTY
  fprintf(output, " ");
  fprintf(output, "%i", ENOTEMPTY);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTEMPTY) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":echrng");
#ifdef ECHRNG
  fprintf(output, " ");
  fprintf(output, "%i", ECHRNG);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":el2nsync");
#ifdef EL2NSYNC
  fprintf(output, " ");
  fprintf(output, "%i", EL2NSYNC);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":el3hlt");
#ifdef EL3HLT
  fprintf(output, " ");
  fprintf(output, "%i", EL3HLT);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":el3rst");
#ifdef EL3RST
  fprintf(output, " ");
  fprintf(output, "%i", EL3RST);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":elnrng");
#ifdef ELNRNG
  fprintf(output, " ");
  fprintf(output, "%i", ELNRNG);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eunatch");
#ifdef EUNATCH
  fprintf(output, " ");
  fprintf(output, "%i", EUNATCH);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enocsi");
#ifdef ENOCSI
  fprintf(output, " ");
  fprintf(output, "%i", ENOCSI);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":el2hlt");
#ifdef EL2HLT
  fprintf(output, " ");
  fprintf(output, "%i", EL2HLT);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebade");
#ifdef EBADE
  fprintf(output, " ");
  fprintf(output, "%i", EBADE);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebadr");
#ifdef EBADR
  fprintf(output, " ");
  fprintf(output, "%i", EBADR);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":exfull");
#ifdef EXFULL
  fprintf(output, " ");
  fprintf(output, "%i", EXFULL);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enoano");
#ifdef ENOANO
  fprintf(output, " ");
  fprintf(output, "%i", ENOANO);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebadrqc");
#ifdef EBADRQC
  fprintf(output, " ");
  fprintf(output, "%i", EBADRQC);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebadslt");
#ifdef EBADSLT
  fprintf(output, " ");
  fprintf(output, "%i", EBADSLT);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":edeadlock");
#ifdef EDEADLOCK
  fprintf(output, " ");
  fprintf(output, "%i", EDEADLOCK);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebfont");
#ifdef EBFONT
  fprintf(output, " ");
  fprintf(output, "%i", EBFONT);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enostr");
#ifdef ENOSTR
  fprintf(output, " ");
  fprintf(output, "%i", ENOSTR);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enodata");
#ifdef ENODATA
  fprintf(output, " ");
  fprintf(output, "%i", ENODATA);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":etime");
#ifdef ETIME
  fprintf(output, " ");
  fprintf(output, "%i", ETIME);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enosr");
#ifdef ENOSR
  fprintf(output, " ");
  fprintf(output, "%i", ENOSR);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enopkg");
#ifdef ENOPKG
  fprintf(output, " ");
  fprintf(output, "%i", ENOPKG);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eadv");
#ifdef EADV
  fprintf(output, " ");
  fprintf(output, "%i", EADV);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":esrmnt");
#ifdef ESRMNT
  fprintf(output, " ");
  fprintf(output, "%i", ESRMNT);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ecomm");
#ifdef ECOMM
  fprintf(output, " ");
  fprintf(output, "%i", ECOMM);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":edotdot");
#ifdef EDOTDOT
  fprintf(output, " ");
  fprintf(output, "%i", EDOTDOT);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotuniq");
#ifdef ENOTUNIQ
  fprintf(output, " ");
  fprintf(output, "%i", ENOTUNIQ);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebadfd");
#ifdef EBADFD
  fprintf(output, " ");
  fprintf(output, "%i", EBADFD);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eremchg");
#ifdef EREMCHG
  fprintf(output, " ");
  fprintf(output, "%i", EREMCHG);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":elibacc");
#ifdef ELIBACC
  fprintf(output, " ");
  fprintf(output, "%i", ELIBACC);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":elibbad");
#ifdef ELIBBAD
  fprintf(output, " ");
  fprintf(output, "%i", ELIBBAD);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":elibscn");
#ifdef ELIBSCN
  fprintf(output, " ");
  fprintf(output, "%i", ELIBSCN);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":elibmax");
#ifdef ELIBMAX
  fprintf(output, " ");
  fprintf(output, "%i", ELIBMAX);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":elibexec");
#ifdef ELIBEXEC
  fprintf(output, " ");
  fprintf(output, "%i", ELIBEXEC);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eilseq");
#ifdef EILSEQ
  fprintf(output, " ");
  fprintf(output, "%i", EILSEQ);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EILSEQ) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":erestart");
#ifdef ERESTART
  fprintf(output, " ");
  fprintf(output, "%i", ERESTART);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":estrpipe");
#ifdef ESTRPIPE
  fprintf(output, " ");
  fprintf(output, "%i", ESTRPIPE);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":euclean");
#ifdef EUCLEAN
  fprintf(output, " ");
  fprintf(output, "%i", EUCLEAN);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotnam");
#ifdef ENOTNAM
  fprintf(output, " ");
  fprintf(output, "%i", ENOTNAM);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enavail");
#ifdef ENAVAIL
  fprintf(output, " ");
  fprintf(output, "%i", ENAVAIL);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eremoteio");
#ifdef EREMOTEIO
  fprintf(output, " ");
  fprintf(output, "%i", EREMOTEIO);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enomedium");
#ifdef ENOMEDIUM
  fprintf(output, " ");
  fprintf(output, "%i", ENOMEDIUM);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":emediumtype");
#ifdef EMEDIUMTYPE
  fprintf(output, " ");
  fprintf(output, "%i", EMEDIUMTYPE);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":estale");
#ifdef ESTALE
  fprintf(output, " ");
  fprintf(output, "%i", ESTALE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ESTALE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotblk");
#ifdef ENOTBLK
  fprintf(output, " ");
  fprintf(output, "%i", ENOTBLK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTBLK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":etxtbsy");
#ifdef ETXTBSY
  fprintf(output, " ");
  fprintf(output, "%i", ETXTBSY);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ETXTBSY) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eusers");
#ifdef EUSERS
  fprintf(output, " ");
  fprintf(output, "%i", EUSERS);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EUSERS) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eloop");
#ifdef ELOOP
  fprintf(output, " ");
  fprintf(output, "%i", ELOOP);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ELOOP) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ewouldblock");
#ifdef EWOULDBLOCK
  fprintf(output, " ");
  fprintf(output, "%i", EWOULDBLOCK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EWOULDBLOCK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enomsg");
#ifdef ENOMSG
  fprintf(output, " ");
  fprintf(output, "%i", ENOMSG);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOMSG) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eidrm");
#ifdef EIDRM
  fprintf(output, " ");
  fprintf(output, "%i", EIDRM);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EIDRM) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eproto");
#ifdef EPROTO
  fprintf(output, " ");
  fprintf(output, "%i", EPROTO);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EPROTO) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":emultihop");
#ifdef EMULTIHOP
  fprintf(output, " ");
  fprintf(output, "%i", EMULTIHOP);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EMULTIHOP) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ebadmsg");
#ifdef EBADMSG
  fprintf(output, " ");
  fprintf(output, "%i", EBADMSG);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EBADMSG) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eoverflow");
#ifdef EOVERFLOW
  fprintf(output, " ");
  fprintf(output, "%i", EOVERFLOW);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EOVERFLOW) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":edquot");
#ifdef EDQUOT
  fprintf(output, " ");
  fprintf(output, "%i", EDQUOT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EDQUOT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":einprogress");
#ifdef EINPROGRESS
  fprintf(output, " ");
  fprintf(output, "%i", EINPROGRESS);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EINPROGRESS) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ealready");
#ifdef EALREADY
  fprintf(output, " ");
  fprintf(output, "%i", EALREADY);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EALREADY) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eprotonosupport");
#ifdef EPROTONOSUPPORT
  fprintf(output, " ");
  fprintf(output, "%i", EPROTONOSUPPORT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EPROTONOSUPPORT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":esocktnosupport");
#ifdef ESOCKTNOSUPPORT
  fprintf(output, " ");
  fprintf(output, "%i", ESOCKTNOSUPPORT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ESOCKTNOSUPPORT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotsock");
#ifdef ENOTSOCK
  fprintf(output, " ");
  fprintf(output, "%i", ENOTSOCK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTSOCK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":edestaddrreq");
#ifdef EDESTADDRREQ
  fprintf(output, " ");
  fprintf(output, "%i", EDESTADDRREQ);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EDESTADDRREQ) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":emsgsize");
#ifdef EMSGSIZE
  fprintf(output, " ");
  fprintf(output, "%i", EMSGSIZE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EMSGSIZE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eprototype");
#ifdef EPROTOTYPE
  fprintf(output, " ");
  fprintf(output, "%i", EPROTOTYPE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EPROTOTYPE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enoprotoopt");
#ifdef ENOPROTOOPT
  fprintf(output, " ");
  fprintf(output, "%i", ENOPROTOOPT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOPROTOOPT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eremote");
#ifdef EREMOTE
  fprintf(output, " ");
  fprintf(output, "%i", EREMOTE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EREMOTE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enolink");
#ifdef ENOLINK
  fprintf(output, " ");
  fprintf(output, "%i", ENOLINK);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOLINK) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":epfnosupport");
#ifdef EPFNOSUPPORT
  fprintf(output, " ");
  fprintf(output, "%i", EPFNOSUPPORT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EPFNOSUPPORT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eafnosupport");
#ifdef EAFNOSUPPORT
  fprintf(output, " ");
  fprintf(output, "%i", EAFNOSUPPORT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EAFNOSUPPORT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eaddrinuse");
#ifdef EADDRINUSE
  fprintf(output, " ");
  fprintf(output, "%i", EADDRINUSE);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EADDRINUSE) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eaddrnotavail");
#ifdef EADDRNOTAVAIL
  fprintf(output, " ");
  fprintf(output, "%i", EADDRNOTAVAIL);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EADDRNOTAVAIL) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enetdown");
#ifdef ENETDOWN
  fprintf(output, " ");
  fprintf(output, "%i", ENETDOWN);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENETDOWN) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enetunreach");
#ifdef ENETUNREACH
  fprintf(output, " ");
  fprintf(output, "%i", ENETUNREACH);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENETUNREACH) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enetreset");
#ifdef ENETRESET
  fprintf(output, " ");
  fprintf(output, "%i", ENETRESET);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENETRESET) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":econnaborted");
#ifdef ECONNABORTED
  fprintf(output, " ");
  fprintf(output, "%i", ECONNABORTED);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ECONNABORTED) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":econnreset");
#ifdef ECONNRESET
  fprintf(output, " ");
  fprintf(output, "%i", ECONNRESET);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ECONNRESET) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eisconn");
#ifdef EISCONN
  fprintf(output, " ");
  fprintf(output, "%i", EISCONN);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EISCONN) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotconn");
#ifdef ENOTCONN
  fprintf(output, " ");
  fprintf(output, "%i", ENOTCONN);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTCONN) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eshutdown");
#ifdef ESHUTDOWN
  fprintf(output, " ");
  fprintf(output, "%i", ESHUTDOWN);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ESHUTDOWN) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":etoomanyrefs");
#ifdef ETOOMANYREFS
  fprintf(output, " ");
  fprintf(output, "%i", ETOOMANYREFS);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ETOOMANYREFS) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":etimedout");
#ifdef ETIMEDOUT
  fprintf(output, " ");
  fprintf(output, "%i", ETIMEDOUT);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ETIMEDOUT) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":econnrefused");
#ifdef ECONNREFUSED
  fprintf(output, " ");
  fprintf(output, "%i", ECONNREFUSED);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ECONNREFUSED) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ehostdown");
#ifdef EHOSTDOWN
  fprintf(output, " ");
  fprintf(output, "%i", EHOSTDOWN);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EHOSTDOWN) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":ehostunreach");
#ifdef EHOSTUNREACH
  fprintf(output, " ");
  fprintf(output, "%i", EHOSTUNREACH);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EHOSTUNREACH) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enonet");
#ifdef ENONET
  fprintf(output, " ");
  fprintf(output, "%i", ENONET);
#else
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enobufs");
#ifdef ENOBUFS
  fprintf(output, " ");
  fprintf(output, "%i", ENOBUFS);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOBUFS) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":enotsup");
#ifdef ENOTSUP
  fprintf(output, " ");
  fprintf(output, "%i", ENOTSUP);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'ENOTSUP) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, "\n  (");
  fprintf(output, ":eopnotsupp");
#ifdef EOPNOTSUPP
  fprintf(output, " ");
  fprintf(output, "%i", EOPNOTSUPP);
#else
  fprintf(output, "\n  #.(cl:progn (cl:warn 'cffi-grovel:missing-definition :name 'EOPNOTSUPP) -1)");
#endif
  fprintf(output, ")");
  fprintf(output, ")\n");

  /* constant section for EPERM */
#ifdef EPERM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eperm");
  fprintf(output, " ");
  if(SIGNED64P(EPERM))
    fprintf(output, "%lli", (int64_t) EPERM);
  else
    fprintf(output, "%llu", (uint64_t) EPERM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPERM)\n");
#endif

  /* constant section for ENOENT */
#ifdef ENOENT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enoent");
  fprintf(output, " ");
  if(SIGNED64P(ENOENT))
    fprintf(output, "%lli", (int64_t) ENOENT);
  else
    fprintf(output, "%llu", (uint64_t) ENOENT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOENT)\n");
#endif

  /* constant section for ESRCH */
#ifdef ESRCH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "esrch");
  fprintf(output, " ");
  if(SIGNED64P(ESRCH))
    fprintf(output, "%lli", (int64_t) ESRCH);
  else
    fprintf(output, "%llu", (uint64_t) ESRCH);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ESRCH)\n");
#endif

  /* constant section for EINTR */
#ifdef EINTR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eintr");
  fprintf(output, " ");
  if(SIGNED64P(EINTR))
    fprintf(output, "%lli", (int64_t) EINTR);
  else
    fprintf(output, "%llu", (uint64_t) EINTR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EINTR)\n");
#endif

  /* constant section for EIO */
#ifdef EIO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eio");
  fprintf(output, " ");
  if(SIGNED64P(EIO))
    fprintf(output, "%lli", (int64_t) EIO);
  else
    fprintf(output, "%llu", (uint64_t) EIO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EIO)\n");
#endif

  /* constant section for ENXIO */
#ifdef ENXIO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enxio");
  fprintf(output, " ");
  if(SIGNED64P(ENXIO))
    fprintf(output, "%lli", (int64_t) ENXIO);
  else
    fprintf(output, "%llu", (uint64_t) ENXIO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENXIO)\n");
#endif

  /* constant section for E2BIG */
#ifdef E2BIG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "e2big");
  fprintf(output, " ");
  if(SIGNED64P(E2BIG))
    fprintf(output, "%lli", (int64_t) E2BIG);
  else
    fprintf(output, "%llu", (uint64_t) E2BIG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'E2BIG)\n");
#endif

  /* constant section for ENOEXEC */
#ifdef ENOEXEC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enoexec");
  fprintf(output, " ");
  if(SIGNED64P(ENOEXEC))
    fprintf(output, "%lli", (int64_t) ENOEXEC);
  else
    fprintf(output, "%llu", (uint64_t) ENOEXEC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOEXEC)\n");
#endif

  /* constant section for EBADF */
#ifdef EBADF
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebadf");
  fprintf(output, " ");
  if(SIGNED64P(EBADF))
    fprintf(output, "%lli", (int64_t) EBADF);
  else
    fprintf(output, "%llu", (uint64_t) EBADF);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EBADF)\n");
#endif

  /* constant section for ECHILD */
#ifdef ECHILD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "echild");
  fprintf(output, " ");
  if(SIGNED64P(ECHILD))
    fprintf(output, "%lli", (int64_t) ECHILD);
  else
    fprintf(output, "%llu", (uint64_t) ECHILD);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ECHILD)\n");
#endif

  /* constant section for ENOMEM */
#ifdef ENOMEM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enomem");
  fprintf(output, " ");
  if(SIGNED64P(ENOMEM))
    fprintf(output, "%lli", (int64_t) ENOMEM);
  else
    fprintf(output, "%llu", (uint64_t) ENOMEM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOMEM)\n");
#endif

  /* constant section for EACCES */
#ifdef EACCES
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eacces");
  fprintf(output, " ");
  if(SIGNED64P(EACCES))
    fprintf(output, "%lli", (int64_t) EACCES);
  else
    fprintf(output, "%llu", (uint64_t) EACCES);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EACCES)\n");
#endif

  /* constant section for EFAULT */
#ifdef EFAULT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "efault");
  fprintf(output, " ");
  if(SIGNED64P(EFAULT))
    fprintf(output, "%lli", (int64_t) EFAULT);
  else
    fprintf(output, "%llu", (uint64_t) EFAULT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EFAULT)\n");
#endif

  /* constant section for EBUSY */
#ifdef EBUSY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebusy");
  fprintf(output, " ");
  if(SIGNED64P(EBUSY))
    fprintf(output, "%lli", (int64_t) EBUSY);
  else
    fprintf(output, "%llu", (uint64_t) EBUSY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EBUSY)\n");
#endif

  /* constant section for EEXIST */
#ifdef EEXIST
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eexist");
  fprintf(output, " ");
  if(SIGNED64P(EEXIST))
    fprintf(output, "%lli", (int64_t) EEXIST);
  else
    fprintf(output, "%llu", (uint64_t) EEXIST);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EEXIST)\n");
#endif

  /* constant section for EXDEV */
#ifdef EXDEV
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "exdev");
  fprintf(output, " ");
  if(SIGNED64P(EXDEV))
    fprintf(output, "%lli", (int64_t) EXDEV);
  else
    fprintf(output, "%llu", (uint64_t) EXDEV);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EXDEV)\n");
#endif

  /* constant section for ENODEV */
#ifdef ENODEV
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enodev");
  fprintf(output, " ");
  if(SIGNED64P(ENODEV))
    fprintf(output, "%lli", (int64_t) ENODEV);
  else
    fprintf(output, "%llu", (uint64_t) ENODEV);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENODEV)\n");
#endif

  /* constant section for ENOTDIR */
#ifdef ENOTDIR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotdir");
  fprintf(output, " ");
  if(SIGNED64P(ENOTDIR))
    fprintf(output, "%lli", (int64_t) ENOTDIR);
  else
    fprintf(output, "%llu", (uint64_t) ENOTDIR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTDIR)\n");
#endif

  /* constant section for EISDIR */
#ifdef EISDIR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eisdir");
  fprintf(output, " ");
  if(SIGNED64P(EISDIR))
    fprintf(output, "%lli", (int64_t) EISDIR);
  else
    fprintf(output, "%llu", (uint64_t) EISDIR);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EISDIR)\n");
#endif

  /* constant section for EINVAL */
#ifdef EINVAL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "einval");
  fprintf(output, " ");
  if(SIGNED64P(EINVAL))
    fprintf(output, "%lli", (int64_t) EINVAL);
  else
    fprintf(output, "%llu", (uint64_t) EINVAL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EINVAL)\n");
#endif

  /* constant section for ENFILE */
#ifdef ENFILE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enfile");
  fprintf(output, " ");
  if(SIGNED64P(ENFILE))
    fprintf(output, "%lli", (int64_t) ENFILE);
  else
    fprintf(output, "%llu", (uint64_t) ENFILE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENFILE)\n");
#endif

  /* constant section for EMFILE */
#ifdef EMFILE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "emfile");
  fprintf(output, " ");
  if(SIGNED64P(EMFILE))
    fprintf(output, "%lli", (int64_t) EMFILE);
  else
    fprintf(output, "%llu", (uint64_t) EMFILE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EMFILE)\n");
#endif

  /* constant section for ENOTTY */
#ifdef ENOTTY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotty");
  fprintf(output, " ");
  if(SIGNED64P(ENOTTY))
    fprintf(output, "%lli", (int64_t) ENOTTY);
  else
    fprintf(output, "%llu", (uint64_t) ENOTTY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTTY)\n");
#endif

  /* constant section for EFBIG */
#ifdef EFBIG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "efbig");
  fprintf(output, " ");
  if(SIGNED64P(EFBIG))
    fprintf(output, "%lli", (int64_t) EFBIG);
  else
    fprintf(output, "%llu", (uint64_t) EFBIG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EFBIG)\n");
#endif

  /* constant section for ENOSPC */
#ifdef ENOSPC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enospc");
  fprintf(output, " ");
  if(SIGNED64P(ENOSPC))
    fprintf(output, "%lli", (int64_t) ENOSPC);
  else
    fprintf(output, "%llu", (uint64_t) ENOSPC);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOSPC)\n");
#endif

  /* constant section for ESPIPE */
#ifdef ESPIPE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "espipe");
  fprintf(output, " ");
  if(SIGNED64P(ESPIPE))
    fprintf(output, "%lli", (int64_t) ESPIPE);
  else
    fprintf(output, "%llu", (uint64_t) ESPIPE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ESPIPE)\n");
#endif

  /* constant section for EROFS */
#ifdef EROFS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "erofs");
  fprintf(output, " ");
  if(SIGNED64P(EROFS))
    fprintf(output, "%lli", (int64_t) EROFS);
  else
    fprintf(output, "%llu", (uint64_t) EROFS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EROFS)\n");
#endif

  /* constant section for EMLINK */
#ifdef EMLINK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "emlink");
  fprintf(output, " ");
  if(SIGNED64P(EMLINK))
    fprintf(output, "%lli", (int64_t) EMLINK);
  else
    fprintf(output, "%llu", (uint64_t) EMLINK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EMLINK)\n");
#endif

  /* constant section for EPIPE */
#ifdef EPIPE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epipe");
  fprintf(output, " ");
  if(SIGNED64P(EPIPE))
    fprintf(output, "%lli", (int64_t) EPIPE);
  else
    fprintf(output, "%llu", (uint64_t) EPIPE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPIPE)\n");
#endif

  /* constant section for EDOM */
#ifdef EDOM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "edom");
  fprintf(output, " ");
  if(SIGNED64P(EDOM))
    fprintf(output, "%lli", (int64_t) EDOM);
  else
    fprintf(output, "%llu", (uint64_t) EDOM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EDOM)\n");
#endif

  /* constant section for ERANGE */
#ifdef ERANGE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "erange");
  fprintf(output, " ");
  if(SIGNED64P(ERANGE))
    fprintf(output, "%lli", (int64_t) ERANGE);
  else
    fprintf(output, "%llu", (uint64_t) ERANGE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ERANGE)\n");
#endif

  /* constant section for EDEADLK */
#ifdef EDEADLK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "edeadlk");
  fprintf(output, " ");
  if(SIGNED64P(EDEADLK))
    fprintf(output, "%lli", (int64_t) EDEADLK);
  else
    fprintf(output, "%llu", (uint64_t) EDEADLK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EDEADLK)\n");
#endif

  /* constant section for ENAMETOOLONG */
#ifdef ENAMETOOLONG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enametoolong");
  fprintf(output, " ");
  if(SIGNED64P(ENAMETOOLONG))
    fprintf(output, "%lli", (int64_t) ENAMETOOLONG);
  else
    fprintf(output, "%llu", (uint64_t) ENAMETOOLONG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENAMETOOLONG)\n");
#endif

  /* constant section for ENOLCK */
#ifdef ENOLCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enolck");
  fprintf(output, " ");
  if(SIGNED64P(ENOLCK))
    fprintf(output, "%lli", (int64_t) ENOLCK);
  else
    fprintf(output, "%llu", (uint64_t) ENOLCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOLCK)\n");
#endif

  /* constant section for ENOSYS */
#ifdef ENOSYS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enosys");
  fprintf(output, " ");
  if(SIGNED64P(ENOSYS))
    fprintf(output, "%lli", (int64_t) ENOSYS);
  else
    fprintf(output, "%llu", (uint64_t) ENOSYS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOSYS)\n");
#endif

  /* constant section for ENOTEMPTY */
#ifdef ENOTEMPTY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotempty");
  fprintf(output, " ");
  if(SIGNED64P(ENOTEMPTY))
    fprintf(output, "%lli", (int64_t) ENOTEMPTY);
  else
    fprintf(output, "%llu", (uint64_t) ENOTEMPTY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTEMPTY)\n");
#endif

  /* constant section for ECHRNG */
#ifdef ECHRNG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "echrng");
  fprintf(output, " ");
  if(SIGNED64P(ECHRNG))
    fprintf(output, "%lli", (int64_t) ECHRNG);
  else
    fprintf(output, "%llu", (uint64_t) ECHRNG);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EL2NSYNC */
#ifdef EL2NSYNC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "el2nsync");
  fprintf(output, " ");
  if(SIGNED64P(EL2NSYNC))
    fprintf(output, "%lli", (int64_t) EL2NSYNC);
  else
    fprintf(output, "%llu", (uint64_t) EL2NSYNC);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EL3HLT */
#ifdef EL3HLT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "el3hlt");
  fprintf(output, " ");
  if(SIGNED64P(EL3HLT))
    fprintf(output, "%lli", (int64_t) EL3HLT);
  else
    fprintf(output, "%llu", (uint64_t) EL3HLT);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EL3RST */
#ifdef EL3RST
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "el3rst");
  fprintf(output, " ");
  if(SIGNED64P(EL3RST))
    fprintf(output, "%lli", (int64_t) EL3RST);
  else
    fprintf(output, "%llu", (uint64_t) EL3RST);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ELNRNG */
#ifdef ELNRNG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "elnrng");
  fprintf(output, " ");
  if(SIGNED64P(ELNRNG))
    fprintf(output, "%lli", (int64_t) ELNRNG);
  else
    fprintf(output, "%llu", (uint64_t) ELNRNG);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EUNATCH */
#ifdef EUNATCH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eunatch");
  fprintf(output, " ");
  if(SIGNED64P(EUNATCH))
    fprintf(output, "%lli", (int64_t) EUNATCH);
  else
    fprintf(output, "%llu", (uint64_t) EUNATCH);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOCSI */
#ifdef ENOCSI
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enocsi");
  fprintf(output, " ");
  if(SIGNED64P(ENOCSI))
    fprintf(output, "%lli", (int64_t) ENOCSI);
  else
    fprintf(output, "%llu", (uint64_t) ENOCSI);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EL2HLT */
#ifdef EL2HLT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "el2hlt");
  fprintf(output, " ");
  if(SIGNED64P(EL2HLT))
    fprintf(output, "%lli", (int64_t) EL2HLT);
  else
    fprintf(output, "%llu", (uint64_t) EL2HLT);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EBADE */
#ifdef EBADE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebade");
  fprintf(output, " ");
  if(SIGNED64P(EBADE))
    fprintf(output, "%lli", (int64_t) EBADE);
  else
    fprintf(output, "%llu", (uint64_t) EBADE);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EBADR */
#ifdef EBADR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebadr");
  fprintf(output, " ");
  if(SIGNED64P(EBADR))
    fprintf(output, "%lli", (int64_t) EBADR);
  else
    fprintf(output, "%llu", (uint64_t) EBADR);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EXFULL */
#ifdef EXFULL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "exfull");
  fprintf(output, " ");
  if(SIGNED64P(EXFULL))
    fprintf(output, "%lli", (int64_t) EXFULL);
  else
    fprintf(output, "%llu", (uint64_t) EXFULL);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOANO */
#ifdef ENOANO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enoano");
  fprintf(output, " ");
  if(SIGNED64P(ENOANO))
    fprintf(output, "%lli", (int64_t) ENOANO);
  else
    fprintf(output, "%llu", (uint64_t) ENOANO);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EBADRQC */
#ifdef EBADRQC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebadrqc");
  fprintf(output, " ");
  if(SIGNED64P(EBADRQC))
    fprintf(output, "%lli", (int64_t) EBADRQC);
  else
    fprintf(output, "%llu", (uint64_t) EBADRQC);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EBADSLT */
#ifdef EBADSLT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebadslt");
  fprintf(output, " ");
  if(SIGNED64P(EBADSLT))
    fprintf(output, "%lli", (int64_t) EBADSLT);
  else
    fprintf(output, "%llu", (uint64_t) EBADSLT);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EDEADLOCK */
#ifdef EDEADLOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "edeadlock");
  fprintf(output, " ");
  if(SIGNED64P(EDEADLOCK))
    fprintf(output, "%lli", (int64_t) EDEADLOCK);
  else
    fprintf(output, "%llu", (uint64_t) EDEADLOCK);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EBFONT */
#ifdef EBFONT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebfont");
  fprintf(output, " ");
  if(SIGNED64P(EBFONT))
    fprintf(output, "%lli", (int64_t) EBFONT);
  else
    fprintf(output, "%llu", (uint64_t) EBFONT);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOSTR */
#ifdef ENOSTR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enostr");
  fprintf(output, " ");
  if(SIGNED64P(ENOSTR))
    fprintf(output, "%lli", (int64_t) ENOSTR);
  else
    fprintf(output, "%llu", (uint64_t) ENOSTR);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENODATA */
#ifdef ENODATA
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enodata");
  fprintf(output, " ");
  if(SIGNED64P(ENODATA))
    fprintf(output, "%lli", (int64_t) ENODATA);
  else
    fprintf(output, "%llu", (uint64_t) ENODATA);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ETIME */
#ifdef ETIME
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "etime");
  fprintf(output, " ");
  if(SIGNED64P(ETIME))
    fprintf(output, "%lli", (int64_t) ETIME);
  else
    fprintf(output, "%llu", (uint64_t) ETIME);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOSR */
#ifdef ENOSR
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enosr");
  fprintf(output, " ");
  if(SIGNED64P(ENOSR))
    fprintf(output, "%lli", (int64_t) ENOSR);
  else
    fprintf(output, "%llu", (uint64_t) ENOSR);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOPKG */
#ifdef ENOPKG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enopkg");
  fprintf(output, " ");
  if(SIGNED64P(ENOPKG))
    fprintf(output, "%lli", (int64_t) ENOPKG);
  else
    fprintf(output, "%llu", (uint64_t) ENOPKG);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EADV */
#ifdef EADV
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eadv");
  fprintf(output, " ");
  if(SIGNED64P(EADV))
    fprintf(output, "%lli", (int64_t) EADV);
  else
    fprintf(output, "%llu", (uint64_t) EADV);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ESRMNT */
#ifdef ESRMNT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "esrmnt");
  fprintf(output, " ");
  if(SIGNED64P(ESRMNT))
    fprintf(output, "%lli", (int64_t) ESRMNT);
  else
    fprintf(output, "%llu", (uint64_t) ESRMNT);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ECOMM */
#ifdef ECOMM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ecomm");
  fprintf(output, " ");
  if(SIGNED64P(ECOMM))
    fprintf(output, "%lli", (int64_t) ECOMM);
  else
    fprintf(output, "%llu", (uint64_t) ECOMM);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EDOTDOT */
#ifdef EDOTDOT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "edotdot");
  fprintf(output, " ");
  if(SIGNED64P(EDOTDOT))
    fprintf(output, "%lli", (int64_t) EDOTDOT);
  else
    fprintf(output, "%llu", (uint64_t) EDOTDOT);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOTUNIQ */
#ifdef ENOTUNIQ
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotuniq");
  fprintf(output, " ");
  if(SIGNED64P(ENOTUNIQ))
    fprintf(output, "%lli", (int64_t) ENOTUNIQ);
  else
    fprintf(output, "%llu", (uint64_t) ENOTUNIQ);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EBADFD */
#ifdef EBADFD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebadfd");
  fprintf(output, " ");
  if(SIGNED64P(EBADFD))
    fprintf(output, "%lli", (int64_t) EBADFD);
  else
    fprintf(output, "%llu", (uint64_t) EBADFD);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EREMCHG */
#ifdef EREMCHG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eremchg");
  fprintf(output, " ");
  if(SIGNED64P(EREMCHG))
    fprintf(output, "%lli", (int64_t) EREMCHG);
  else
    fprintf(output, "%llu", (uint64_t) EREMCHG);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ELIBACC */
#ifdef ELIBACC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "elibacc");
  fprintf(output, " ");
  if(SIGNED64P(ELIBACC))
    fprintf(output, "%lli", (int64_t) ELIBACC);
  else
    fprintf(output, "%llu", (uint64_t) ELIBACC);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ELIBBAD */
#ifdef ELIBBAD
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "elibbad");
  fprintf(output, " ");
  if(SIGNED64P(ELIBBAD))
    fprintf(output, "%lli", (int64_t) ELIBBAD);
  else
    fprintf(output, "%llu", (uint64_t) ELIBBAD);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ELIBSCN */
#ifdef ELIBSCN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "elibscn");
  fprintf(output, " ");
  if(SIGNED64P(ELIBSCN))
    fprintf(output, "%lli", (int64_t) ELIBSCN);
  else
    fprintf(output, "%llu", (uint64_t) ELIBSCN);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ELIBMAX */
#ifdef ELIBMAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "elibmax");
  fprintf(output, " ");
  if(SIGNED64P(ELIBMAX))
    fprintf(output, "%lli", (int64_t) ELIBMAX);
  else
    fprintf(output, "%llu", (uint64_t) ELIBMAX);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ELIBEXEC */
#ifdef ELIBEXEC
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "elibexec");
  fprintf(output, " ");
  if(SIGNED64P(ELIBEXEC))
    fprintf(output, "%lli", (int64_t) ELIBEXEC);
  else
    fprintf(output, "%llu", (uint64_t) ELIBEXEC);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EILSEQ */
#ifdef EILSEQ
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eilseq");
  fprintf(output, " ");
  if(SIGNED64P(EILSEQ))
    fprintf(output, "%lli", (int64_t) EILSEQ);
  else
    fprintf(output, "%llu", (uint64_t) EILSEQ);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EILSEQ)\n");
#endif

  /* constant section for ERESTART */
#ifdef ERESTART
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "erestart");
  fprintf(output, " ");
  if(SIGNED64P(ERESTART))
    fprintf(output, "%lli", (int64_t) ERESTART);
  else
    fprintf(output, "%llu", (uint64_t) ERESTART);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ESTRPIPE */
#ifdef ESTRPIPE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "estrpipe");
  fprintf(output, " ");
  if(SIGNED64P(ESTRPIPE))
    fprintf(output, "%lli", (int64_t) ESTRPIPE);
  else
    fprintf(output, "%llu", (uint64_t) ESTRPIPE);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EUCLEAN */
#ifdef EUCLEAN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "euclean");
  fprintf(output, " ");
  if(SIGNED64P(EUCLEAN))
    fprintf(output, "%lli", (int64_t) EUCLEAN);
  else
    fprintf(output, "%llu", (uint64_t) EUCLEAN);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOTNAM */
#ifdef ENOTNAM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotnam");
  fprintf(output, " ");
  if(SIGNED64P(ENOTNAM))
    fprintf(output, "%lli", (int64_t) ENOTNAM);
  else
    fprintf(output, "%llu", (uint64_t) ENOTNAM);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENAVAIL */
#ifdef ENAVAIL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enavail");
  fprintf(output, " ");
  if(SIGNED64P(ENAVAIL))
    fprintf(output, "%lli", (int64_t) ENAVAIL);
  else
    fprintf(output, "%llu", (uint64_t) ENAVAIL);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EREMOTEIO */
#ifdef EREMOTEIO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eremoteio");
  fprintf(output, " ");
  if(SIGNED64P(EREMOTEIO))
    fprintf(output, "%lli", (int64_t) EREMOTEIO);
  else
    fprintf(output, "%llu", (uint64_t) EREMOTEIO);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOMEDIUM */
#ifdef ENOMEDIUM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enomedium");
  fprintf(output, " ");
  if(SIGNED64P(ENOMEDIUM))
    fprintf(output, "%lli", (int64_t) ENOMEDIUM);
  else
    fprintf(output, "%llu", (uint64_t) ENOMEDIUM);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for EMEDIUMTYPE */
#ifdef EMEDIUMTYPE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "emediumtype");
  fprintf(output, " ");
  if(SIGNED64P(EMEDIUMTYPE))
    fprintf(output, "%lli", (int64_t) EMEDIUMTYPE);
  else
    fprintf(output, "%llu", (uint64_t) EMEDIUMTYPE);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ESTALE */
#ifdef ESTALE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "estale");
  fprintf(output, " ");
  if(SIGNED64P(ESTALE))
    fprintf(output, "%lli", (int64_t) ESTALE);
  else
    fprintf(output, "%llu", (uint64_t) ESTALE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ESTALE)\n");
#endif

  /* constant section for ENOTBLK */
#ifdef ENOTBLK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotblk");
  fprintf(output, " ");
  if(SIGNED64P(ENOTBLK))
    fprintf(output, "%lli", (int64_t) ENOTBLK);
  else
    fprintf(output, "%llu", (uint64_t) ENOTBLK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTBLK)\n");
#endif

  /* constant section for ETXTBSY */
#ifdef ETXTBSY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "etxtbsy");
  fprintf(output, " ");
  if(SIGNED64P(ETXTBSY))
    fprintf(output, "%lli", (int64_t) ETXTBSY);
  else
    fprintf(output, "%llu", (uint64_t) ETXTBSY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ETXTBSY)\n");
#endif

  /* constant section for EUSERS */
#ifdef EUSERS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eusers");
  fprintf(output, " ");
  if(SIGNED64P(EUSERS))
    fprintf(output, "%lli", (int64_t) EUSERS);
  else
    fprintf(output, "%llu", (uint64_t) EUSERS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EUSERS)\n");
#endif

  /* constant section for ELOOP */
#ifdef ELOOP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eloop");
  fprintf(output, " ");
  if(SIGNED64P(ELOOP))
    fprintf(output, "%lli", (int64_t) ELOOP);
  else
    fprintf(output, "%llu", (uint64_t) ELOOP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ELOOP)\n");
#endif

  /* constant section for EWOULDBLOCK */
#ifdef EWOULDBLOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ewouldblock");
  fprintf(output, " ");
  if(SIGNED64P(EWOULDBLOCK))
    fprintf(output, "%lli", (int64_t) EWOULDBLOCK);
  else
    fprintf(output, "%llu", (uint64_t) EWOULDBLOCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EWOULDBLOCK)\n");
#endif

  /* constant section for ENOMSG */
#ifdef ENOMSG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enomsg");
  fprintf(output, " ");
  if(SIGNED64P(ENOMSG))
    fprintf(output, "%lli", (int64_t) ENOMSG);
  else
    fprintf(output, "%llu", (uint64_t) ENOMSG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOMSG)\n");
#endif

  /* constant section for EIDRM */
#ifdef EIDRM
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eidrm");
  fprintf(output, " ");
  if(SIGNED64P(EIDRM))
    fprintf(output, "%lli", (int64_t) EIDRM);
  else
    fprintf(output, "%llu", (uint64_t) EIDRM);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EIDRM)\n");
#endif

  /* constant section for EPROTO */
#ifdef EPROTO
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eproto");
  fprintf(output, " ");
  if(SIGNED64P(EPROTO))
    fprintf(output, "%lli", (int64_t) EPROTO);
  else
    fprintf(output, "%llu", (uint64_t) EPROTO);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPROTO)\n");
#endif

  /* constant section for EMULTIHOP */
#ifdef EMULTIHOP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "emultihop");
  fprintf(output, " ");
  if(SIGNED64P(EMULTIHOP))
    fprintf(output, "%lli", (int64_t) EMULTIHOP);
  else
    fprintf(output, "%llu", (uint64_t) EMULTIHOP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EMULTIHOP)\n");
#endif

  /* constant section for EBADMSG */
#ifdef EBADMSG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ebadmsg");
  fprintf(output, " ");
  if(SIGNED64P(EBADMSG))
    fprintf(output, "%lli", (int64_t) EBADMSG);
  else
    fprintf(output, "%llu", (uint64_t) EBADMSG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EBADMSG)\n");
#endif

  /* constant section for EOVERFLOW */
#ifdef EOVERFLOW
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eoverflow");
  fprintf(output, " ");
  if(SIGNED64P(EOVERFLOW))
    fprintf(output, "%lli", (int64_t) EOVERFLOW);
  else
    fprintf(output, "%llu", (uint64_t) EOVERFLOW);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EOVERFLOW)\n");
#endif

  /* constant section for EDQUOT */
#ifdef EDQUOT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "edquot");
  fprintf(output, " ");
  if(SIGNED64P(EDQUOT))
    fprintf(output, "%lli", (int64_t) EDQUOT);
  else
    fprintf(output, "%llu", (uint64_t) EDQUOT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EDQUOT)\n");
#endif

  /* constant section for EINPROGRESS */
#ifdef EINPROGRESS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "einprogress");
  fprintf(output, " ");
  if(SIGNED64P(EINPROGRESS))
    fprintf(output, "%lli", (int64_t) EINPROGRESS);
  else
    fprintf(output, "%llu", (uint64_t) EINPROGRESS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EINPROGRESS)\n");
#endif

  /* constant section for EALREADY */
#ifdef EALREADY
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ealready");
  fprintf(output, " ");
  if(SIGNED64P(EALREADY))
    fprintf(output, "%lli", (int64_t) EALREADY);
  else
    fprintf(output, "%llu", (uint64_t) EALREADY);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EALREADY)\n");
#endif

  /* constant section for EPROTONOSUPPORT */
#ifdef EPROTONOSUPPORT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eprotonosupport");
  fprintf(output, " ");
  if(SIGNED64P(EPROTONOSUPPORT))
    fprintf(output, "%lli", (int64_t) EPROTONOSUPPORT);
  else
    fprintf(output, "%llu", (uint64_t) EPROTONOSUPPORT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPROTONOSUPPORT)\n");
#endif

  /* constant section for ESOCKTNOSUPPORT */
#ifdef ESOCKTNOSUPPORT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "esocktnosupport");
  fprintf(output, " ");
  if(SIGNED64P(ESOCKTNOSUPPORT))
    fprintf(output, "%lli", (int64_t) ESOCKTNOSUPPORT);
  else
    fprintf(output, "%llu", (uint64_t) ESOCKTNOSUPPORT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ESOCKTNOSUPPORT)\n");
#endif

  /* constant section for ENOTSOCK */
#ifdef ENOTSOCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotsock");
  fprintf(output, " ");
  if(SIGNED64P(ENOTSOCK))
    fprintf(output, "%lli", (int64_t) ENOTSOCK);
  else
    fprintf(output, "%llu", (uint64_t) ENOTSOCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTSOCK)\n");
#endif

  /* constant section for EDESTADDRREQ */
#ifdef EDESTADDRREQ
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "edestaddrreq");
  fprintf(output, " ");
  if(SIGNED64P(EDESTADDRREQ))
    fprintf(output, "%lli", (int64_t) EDESTADDRREQ);
  else
    fprintf(output, "%llu", (uint64_t) EDESTADDRREQ);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EDESTADDRREQ)\n");
#endif

  /* constant section for EMSGSIZE */
#ifdef EMSGSIZE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "emsgsize");
  fprintf(output, " ");
  if(SIGNED64P(EMSGSIZE))
    fprintf(output, "%lli", (int64_t) EMSGSIZE);
  else
    fprintf(output, "%llu", (uint64_t) EMSGSIZE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EMSGSIZE)\n");
#endif

  /* constant section for EPROTOTYPE */
#ifdef EPROTOTYPE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eprototype");
  fprintf(output, " ");
  if(SIGNED64P(EPROTOTYPE))
    fprintf(output, "%lli", (int64_t) EPROTOTYPE);
  else
    fprintf(output, "%llu", (uint64_t) EPROTOTYPE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPROTOTYPE)\n");
#endif

  /* constant section for ENOPROTOOPT */
#ifdef ENOPROTOOPT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enoprotoopt");
  fprintf(output, " ");
  if(SIGNED64P(ENOPROTOOPT))
    fprintf(output, "%lli", (int64_t) ENOPROTOOPT);
  else
    fprintf(output, "%llu", (uint64_t) ENOPROTOOPT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOPROTOOPT)\n");
#endif

  /* constant section for EREMOTE */
#ifdef EREMOTE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eremote");
  fprintf(output, " ");
  if(SIGNED64P(EREMOTE))
    fprintf(output, "%lli", (int64_t) EREMOTE);
  else
    fprintf(output, "%llu", (uint64_t) EREMOTE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EREMOTE)\n");
#endif

  /* constant section for ENOLINK */
#ifdef ENOLINK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enolink");
  fprintf(output, " ");
  if(SIGNED64P(ENOLINK))
    fprintf(output, "%lli", (int64_t) ENOLINK);
  else
    fprintf(output, "%llu", (uint64_t) ENOLINK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOLINK)\n");
#endif

  /* constant section for EPFNOSUPPORT */
#ifdef EPFNOSUPPORT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "epfnosupport");
  fprintf(output, " ");
  if(SIGNED64P(EPFNOSUPPORT))
    fprintf(output, "%lli", (int64_t) EPFNOSUPPORT);
  else
    fprintf(output, "%llu", (uint64_t) EPFNOSUPPORT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EPFNOSUPPORT)\n");
#endif

  /* constant section for EAFNOSUPPORT */
#ifdef EAFNOSUPPORT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eafnosupport");
  fprintf(output, " ");
  if(SIGNED64P(EAFNOSUPPORT))
    fprintf(output, "%lli", (int64_t) EAFNOSUPPORT);
  else
    fprintf(output, "%llu", (uint64_t) EAFNOSUPPORT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EAFNOSUPPORT)\n");
#endif

  /* constant section for EADDRINUSE */
#ifdef EADDRINUSE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eaddrinuse");
  fprintf(output, " ");
  if(SIGNED64P(EADDRINUSE))
    fprintf(output, "%lli", (int64_t) EADDRINUSE);
  else
    fprintf(output, "%llu", (uint64_t) EADDRINUSE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EADDRINUSE)\n");
#endif

  /* constant section for EADDRNOTAVAIL */
#ifdef EADDRNOTAVAIL
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eaddrnotavail");
  fprintf(output, " ");
  if(SIGNED64P(EADDRNOTAVAIL))
    fprintf(output, "%lli", (int64_t) EADDRNOTAVAIL);
  else
    fprintf(output, "%llu", (uint64_t) EADDRNOTAVAIL);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EADDRNOTAVAIL)\n");
#endif

  /* constant section for ENETDOWN */
#ifdef ENETDOWN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enetdown");
  fprintf(output, " ");
  if(SIGNED64P(ENETDOWN))
    fprintf(output, "%lli", (int64_t) ENETDOWN);
  else
    fprintf(output, "%llu", (uint64_t) ENETDOWN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENETDOWN)\n");
#endif

  /* constant section for ENETUNREACH */
#ifdef ENETUNREACH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enetunreach");
  fprintf(output, " ");
  if(SIGNED64P(ENETUNREACH))
    fprintf(output, "%lli", (int64_t) ENETUNREACH);
  else
    fprintf(output, "%llu", (uint64_t) ENETUNREACH);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENETUNREACH)\n");
#endif

  /* constant section for ENETRESET */
#ifdef ENETRESET
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enetreset");
  fprintf(output, " ");
  if(SIGNED64P(ENETRESET))
    fprintf(output, "%lli", (int64_t) ENETRESET);
  else
    fprintf(output, "%llu", (uint64_t) ENETRESET);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENETRESET)\n");
#endif

  /* constant section for ECONNABORTED */
#ifdef ECONNABORTED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "econnaborted");
  fprintf(output, " ");
  if(SIGNED64P(ECONNABORTED))
    fprintf(output, "%lli", (int64_t) ECONNABORTED);
  else
    fprintf(output, "%llu", (uint64_t) ECONNABORTED);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ECONNABORTED)\n");
#endif

  /* constant section for ECONNRESET */
#ifdef ECONNRESET
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "econnreset");
  fprintf(output, " ");
  if(SIGNED64P(ECONNRESET))
    fprintf(output, "%lli", (int64_t) ECONNRESET);
  else
    fprintf(output, "%llu", (uint64_t) ECONNRESET);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ECONNRESET)\n");
#endif

  /* constant section for EISCONN */
#ifdef EISCONN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eisconn");
  fprintf(output, " ");
  if(SIGNED64P(EISCONN))
    fprintf(output, "%lli", (int64_t) EISCONN);
  else
    fprintf(output, "%llu", (uint64_t) EISCONN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EISCONN)\n");
#endif

  /* constant section for ENOTCONN */
#ifdef ENOTCONN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotconn");
  fprintf(output, " ");
  if(SIGNED64P(ENOTCONN))
    fprintf(output, "%lli", (int64_t) ENOTCONN);
  else
    fprintf(output, "%llu", (uint64_t) ENOTCONN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTCONN)\n");
#endif

  /* constant section for ESHUTDOWN */
#ifdef ESHUTDOWN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eshutdown");
  fprintf(output, " ");
  if(SIGNED64P(ESHUTDOWN))
    fprintf(output, "%lli", (int64_t) ESHUTDOWN);
  else
    fprintf(output, "%llu", (uint64_t) ESHUTDOWN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ESHUTDOWN)\n");
#endif

  /* constant section for ETOOMANYREFS */
#ifdef ETOOMANYREFS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "etoomanyrefs");
  fprintf(output, " ");
  if(SIGNED64P(ETOOMANYREFS))
    fprintf(output, "%lli", (int64_t) ETOOMANYREFS);
  else
    fprintf(output, "%llu", (uint64_t) ETOOMANYREFS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ETOOMANYREFS)\n");
#endif

  /* constant section for ETIMEDOUT */
#ifdef ETIMEDOUT
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "etimedout");
  fprintf(output, " ");
  if(SIGNED64P(ETIMEDOUT))
    fprintf(output, "%lli", (int64_t) ETIMEDOUT);
  else
    fprintf(output, "%llu", (uint64_t) ETIMEDOUT);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ETIMEDOUT)\n");
#endif

  /* constant section for ECONNREFUSED */
#ifdef ECONNREFUSED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "econnrefused");
  fprintf(output, " ");
  if(SIGNED64P(ECONNREFUSED))
    fprintf(output, "%lli", (int64_t) ECONNREFUSED);
  else
    fprintf(output, "%llu", (uint64_t) ECONNREFUSED);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ECONNREFUSED)\n");
#endif

  /* constant section for EHOSTDOWN */
#ifdef EHOSTDOWN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ehostdown");
  fprintf(output, " ");
  if(SIGNED64P(EHOSTDOWN))
    fprintf(output, "%lli", (int64_t) EHOSTDOWN);
  else
    fprintf(output, "%llu", (uint64_t) EHOSTDOWN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EHOSTDOWN)\n");
#endif

  /* constant section for EHOSTUNREACH */
#ifdef EHOSTUNREACH
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "ehostunreach");
  fprintf(output, " ");
  if(SIGNED64P(EHOSTUNREACH))
    fprintf(output, "%lli", (int64_t) EHOSTUNREACH);
  else
    fprintf(output, "%llu", (uint64_t) EHOSTUNREACH);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EHOSTUNREACH)\n");
#endif

  /* constant section for ENONET */
#ifdef ENONET
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enonet");
  fprintf(output, " ");
  if(SIGNED64P(ENONET))
    fprintf(output, "%lli", (int64_t) ENONET);
  else
    fprintf(output, "%llu", (uint64_t) ENONET);
  fprintf(output, ")\n");
#else
#endif

  /* constant section for ENOBUFS */
#ifdef ENOBUFS
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enobufs");
  fprintf(output, " ");
  if(SIGNED64P(ENOBUFS))
    fprintf(output, "%lli", (int64_t) ENOBUFS);
  else
    fprintf(output, "%llu", (uint64_t) ENOBUFS);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOBUFS)\n");
#endif

  /* constant section for ENOTSUP */
#ifdef ENOTSUP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "enotsup");
  fprintf(output, " ");
  if(SIGNED64P(ENOTSUP))
    fprintf(output, "%lli", (int64_t) ENOTSUP);
  else
    fprintf(output, "%llu", (uint64_t) ENOTSUP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'ENOTSUP)\n");
#endif

  /* constant section for EOPNOTSUPP */
#ifdef EOPNOTSUPP
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "eopnotsupp");
  fprintf(output, " ");
  if(SIGNED64P(EOPNOTSUPP))
    fprintf(output, "%lli", (int64_t) EOPNOTSUPP);
  else
    fprintf(output, "%llu", (uint64_t) EOPNOTSUPP);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'EOPNOTSUPP)\n");
#endif

  /* constant section for WNOHANG */
#ifdef WNOHANG
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "wnohang");
  fprintf(output, " ");
  if(SIGNED64P(WNOHANG))
    fprintf(output, "%lli", (int64_t) WNOHANG);
  else
    fprintf(output, "%llu", (uint64_t) WNOHANG);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'WNOHANG)\n");
#endif

  /* constant section for WUNTRACED */
#ifdef WUNTRACED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "wuntraced");
  fprintf(output, " ");
  if(SIGNED64P(WUNTRACED))
    fprintf(output, "%lli", (int64_t) WUNTRACED);
  else
    fprintf(output, "%llu", (uint64_t) WUNTRACED);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'WUNTRACED)\n");
#endif

  /* constant section for WCONTINUED */
#ifdef WCONTINUED
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "wcontinued");
  fprintf(output, " ");
  if(SIGNED64P(WCONTINUED))
    fprintf(output, "%lli", (int64_t) WCONTINUED);
  else
    fprintf(output, "%llu", (uint64_t) WCONTINUED);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'WCONTINUED)\n");
#endif

  /* constant section for SC-ARG-MAX */
#ifdef _SC_ARG_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-arg-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_ARG_MAX))
    fprintf(output, "%lli", (int64_t) _SC_ARG_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_ARG_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-ARG-MAX)\n");
#endif

  /* constant section for SC-CHILD-MAX */
#ifdef _SC_CHILD_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-child-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_CHILD_MAX))
    fprintf(output, "%lli", (int64_t) _SC_CHILD_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_CHILD_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-CHILD-MAX)\n");
#endif

  /* constant section for SC-HOST-NAME-MAX */
#ifdef _SC_HOST_NAME_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-host-name-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_HOST_NAME_MAX))
    fprintf(output, "%lli", (int64_t) _SC_HOST_NAME_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_HOST_NAME_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-HOST-NAME-MAX)\n");
#endif

  /* constant section for SC-LOGIN-NAME-MAX */
#ifdef _SC_LOGIN_NAME_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-login-name-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_LOGIN_NAME_MAX))
    fprintf(output, "%lli", (int64_t) _SC_LOGIN_NAME_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_LOGIN_NAME_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-LOGIN-NAME-MAX)\n");
#endif

  /* constant section for SC-CLK-TCK */
#ifdef _SC_CLK_TCK
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-clk-tck");
  fprintf(output, " ");
  if(SIGNED64P(_SC_CLK_TCK))
    fprintf(output, "%lli", (int64_t) _SC_CLK_TCK);
  else
    fprintf(output, "%llu", (uint64_t) _SC_CLK_TCK);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-CLK-TCK)\n");
#endif

  /* constant section for SC-OPEN-MAX */
#ifdef _SC_OPEN_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-open-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_OPEN_MAX))
    fprintf(output, "%lli", (int64_t) _SC_OPEN_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_OPEN_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-OPEN-MAX)\n");
#endif

  /* constant section for SC-PAGESIZE */
#ifdef _SC_PAGESIZE
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-pagesize");
  fprintf(output, " ");
  if(SIGNED64P(_SC_PAGESIZE))
    fprintf(output, "%lli", (int64_t) _SC_PAGESIZE);
  else
    fprintf(output, "%llu", (uint64_t) _SC_PAGESIZE);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-PAGESIZE)\n");
#endif

  /* constant section for SC-RE-DUP-MAX */
#ifdef _SC_RE_DUP_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-re-dup-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_RE_DUP_MAX))
    fprintf(output, "%lli", (int64_t) _SC_RE_DUP_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_RE_DUP_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-RE-DUP-MAX)\n");
#endif

  /* constant section for SC-STREAM-MAX */
#ifdef _SC_STREAM_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-stream-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_STREAM_MAX))
    fprintf(output, "%lli", (int64_t) _SC_STREAM_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_STREAM_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-STREAM-MAX)\n");
#endif

  /* constant section for SC-SYMLOOP-MAX */
#ifdef _SC_SYMLOOP_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-symloop-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_SYMLOOP_MAX))
    fprintf(output, "%lli", (int64_t) _SC_SYMLOOP_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_SYMLOOP_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-SYMLOOP-MAX)\n");
#endif

  /* constant section for SC-TTY-NAME-MAX */
#ifdef _SC_TTY_NAME_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-tty-name-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_TTY_NAME_MAX))
    fprintf(output, "%lli", (int64_t) _SC_TTY_NAME_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_TTY_NAME_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-TTY-NAME-MAX)\n");
#endif

  /* constant section for SC-TZNAME-MAX */
#ifdef _SC_TZNAME_MAX
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-tzname-max");
  fprintf(output, " ");
  if(SIGNED64P(_SC_TZNAME_MAX))
    fprintf(output, "%lli", (int64_t) _SC_TZNAME_MAX);
  else
    fprintf(output, "%llu", (uint64_t) _SC_TZNAME_MAX);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-TZNAME-MAX)\n");
#endif

  /* constant section for SC-VERSION */
#ifdef _SC_VERSION
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-version");
  fprintf(output, " ");
  if(SIGNED64P(_SC_VERSION))
    fprintf(output, "%lli", (int64_t) _SC_VERSION);
  else
    fprintf(output, "%llu", (uint64_t) _SC_VERSION);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-VERSION)\n");
#endif

  /* constant section for SC-PHYS-PAGES */
#ifdef _SC_PHYS_PAGES
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-phys-pages");
  fprintf(output, " ");
  if(SIGNED64P(_SC_PHYS_PAGES))
    fprintf(output, "%lli", (int64_t) _SC_PHYS_PAGES);
  else
    fprintf(output, "%llu", (uint64_t) _SC_PHYS_PAGES);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-PHYS-PAGES)\n");
#endif

  /* constant section for SC-AVPHYS-PAGES */
#ifdef _SC_AVPHYS_PAGES
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-avphys-pages");
  fprintf(output, " ");
  if(SIGNED64P(_SC_AVPHYS_PAGES))
    fprintf(output, "%lli", (int64_t) _SC_AVPHYS_PAGES);
  else
    fprintf(output, "%llu", (uint64_t) _SC_AVPHYS_PAGES);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-AVPHYS-PAGES)\n");
#endif

  /* constant section for SC-NPROCESSORS-CONF */
#ifdef _SC_NPROCESSORS_CONF
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-nprocessors-conf");
  fprintf(output, " ");
  if(SIGNED64P(_SC_NPROCESSORS_CONF))
    fprintf(output, "%lli", (int64_t) _SC_NPROCESSORS_CONF);
  else
    fprintf(output, "%llu", (uint64_t) _SC_NPROCESSORS_CONF);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-NPROCESSORS-CONF)\n");
#endif

  /* constant section for SC-NPROCESSORS-ONLN */
#ifdef _SC_NPROCESSORS_ONLN
  fprintf(output, "(cl:defconstant ");
  fprintf(output, "sc-nprocessors-onln");
  fprintf(output, " ");
  if(SIGNED64P(_SC_NPROCESSORS_ONLN))
    fprintf(output, "%lli", (int64_t) _SC_NPROCESSORS_ONLN);
  else
    fprintf(output, "%llu", (uint64_t) _SC_NPROCESSORS_ONLN);
  fprintf(output, ")\n");
#else
  fprintf(output, "(cl:warn 'cffi-grovel:missing-definition :name 'SC-NPROCESSORS-ONLN)\n");
#endif

  if  (output != stdout)
    fclose(output);
  return 0;
}

void type_name(FILE *output, int signed_p, int size) {
  if (signed_p) {
    switch (size) {
    case 1: fprintf(output, ":int8"); break;
    case 2: fprintf(output, ":int16"); break;
    case 4: fprintf(output, ":int32"); break;
    case 8: fprintf(output, ":int64"); break;
    default: goto error;
    }
  } else {
    switch(size) {
    case 1: fprintf(output, ":uint8"); break;
    case 2: fprintf(output, ":uint16"); break;
    case 4: fprintf(output, ":uint32"); break;
    case 8: fprintf(output, ":uint64"); break;
    default: goto error;
    }
  }

  return;

error:
  fprintf(output, "(cl:error \"No type of size ~D.\" %i)\n", size);
}

char* print_double_for_lisp(double n)
{
    static char buf[256];
    memset(buf, 0, 256);
    snprintf(buf, 255, "(let ((*read-default-float-format* 'double-float)) (coerce (read-from-string \"%.20E\") 'double-float))", n);
    return buf;
}
